# JCS 0.0.26.1 — PC 화이트스크린 핫픽스

## 적용 내용

- 첫 API 응답을 기다리는 동안 빈 화면 대신 정참시 로딩 화면을 즉시 표시합니다.
- 메인 홈의 배지 방문 기록·상태 계산을 본문 렌더와 분리합니다.
- 배지 계산이 끝나면 PC 오른쪽 사이드바와 모바일 배지 영역만 갱신합니다.
- 이전 화면이 이미 표시된 해시 이동에서는 로딩 화면으로 덮어쓰지 않습니다.
- `0.0.26.1` 캐시 키로 변경된 JavaScript와 CSS를 즉시 다시 받도록 했습니다.

## GitHub 적용

ZIP을 푼 뒤 폴더 구조를 유지한 상태로 저장소 루트에 덮어씁니다.

- `index.html`
- `css/app.css`
- `src/app.js`
- `src/layout/home-layout.js`
- `src/layout/site-shell.js`
- `tests/white-screen-hotfix.test.js`

업로드가 끝나면 Vercel 자동 배포 완료 후 PC에서 `Ctrl + Shift + R`로 한 번 새로고침합니다.

## 검증 기준

- 전용 회귀 테스트 8개 통과
- 전체 테스트 163개 중 162개 통과
- 유일한 실패 1개는 기존 사진 개수 기준 불일치(기대 515, 실제 593)이며 이 핫픽스와 무관합니다.
