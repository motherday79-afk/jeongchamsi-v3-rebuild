# JCS 0.0.31.32 적용 안내

## 이번 반영 내용

- 마이페이지 요약은 내가 쓴 글, 획득 배지, 즐겨찾기 글, 즐겨찾기 정치인을 각각 5개만 먼저 표시합니다.
- 내가 쓴 글과 즐겨찾기 글 전체보기는 한 페이지 15개와 숫자 페이지 이동을 사용합니다.
- 배지 전체보기는 56개 배지를 등급별 한 페이지에 표시하고, 획득 배지는 점등·미획득 배지는 소등 상태로 구분합니다.
- 즐겨찾기 정치인 전체보기는 별도 페이지 나눔 없이 NOW 순위형 목록으로 모두 표시합니다.
- 정치인 브랜드 진단은 근거 4개 원형 지표, 10구간 세로 막대, 평균 점수, 5축 확장성과 확장 비율로 표시합니다.
- 지역구 진단은 공식 지역 연령별 유권자 현황과 4단계 메시지 확산 흐름, 실제 지역 기사 반응 매트릭스를 서로 분리합니다.
- 핵심 지지층은 합계 100% 구성이 아닌 코어 결집·유동층 이동·이탈 위험의 독립 JCS 지수로 표시합니다.
- 이슈 지속력은 최근 30일 실제 일별 기사량과 7일 이동 평균, 실제 기사 사건점을 함께 표시합니다.
- 관리자 상세페이지 사진 업로드는 Vercel Blob 토큰을 명시적으로 사용하며 저장소 연결 상태를 화면에 표시합니다.
- 브라우저·서버 릴리스 식별자를 `JCS_0_0_31_32`로 맞췄습니다.

## 배포 직후 확인 순서

1. ZIP을 압축 해제한 뒤 기존 0.0.31.31 프로젝트 최상단에 같은 경로로 덮어씁니다.
2. GitHub Actions가 자동 실행되지 않을 때만 해당 배포 워크플로에서 `Run workflow`를 누릅니다.
3. Vercel 배포 완료 후 브라우저를 강력 새로고침합니다.
4. 관리자 로그인 후 임의 정치인 상세페이지에서 사진 등록 창을 열어 `사진 저장소 연결됨`을 확인합니다.
5. 기존 공개 스냅샷에 지역 연령 자료가 저장되어 있지 않은 정치인은 관리자에서 전체 수집 → 검수 승인 → 게시를 한 번 실행해야 공식 지역 연령별 유권자 현황이 공개 데이터에 들어갑니다.

## 사진 업로드 환경

- Vercel 프로젝트에서 Blob Store를 연결하고 `BLOB_READ_WRITE_TOKEN`이 Production 환경에 있어야 합니다.
- 환경변수를 추가하거나 Blob 연결을 바꾼 뒤에는 반드시 재배포해야 합니다.
- 정상일 때 관리자 정치인 상세페이지 사진 등록 창에 `사진 저장소 연결됨`이 표시됩니다.
- 지원 파일은 JPG·PNG·WEBP이며 최대 1MB입니다.

## 검증 결과

- `npm test`
- 전체 423개 통과, 실패 0개

