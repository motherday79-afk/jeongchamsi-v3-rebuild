# JCS 0.0.31.41 게시 용량 핫픽스

## 적용 후 할 일

1. ZIP을 풀어 기존 프로젝트의 동일 경로에 덮어쓰고 배포합니다.
2. 관리자와 `/api/v3/health`의 버전이 `JCS_0_0_31_41`인지 확인하고 강력 새로고침합니다.
3. **수집을 다시 하지 말고, 관리자 데이터 수집 화면에서 `전체 게시` 또는 `이어서 처리`를 누릅니다.** 이미 완료된 수집 결과를 사용합니다.

## 수정

- 기존 수집 결과를 삭제하거나 새로 수집하지 않고, 저장된 분석 JSON을 같은 키에서 무손실 gzip으로 압축합니다. 이전 JSON과 압축 형식을 모두 읽습니다. 게시 진행 중 새 수집본과 해당 인물의 기존 공개본을 압축해 공간을 확보합니다. 이후 수집 결과도 압축 저장합니다.
- 게시 마지막 순위 데이터에 전체 기사 목록을 다시 넣던 중복 저장을 제거했습니다. 정치키워드는 저장된 수집본을 재사용하며, 배포 서버의 60초 캐시로 반복 조회를 줄입니다. 외부 재수집을 하지 않습니다.
- 기존 공개 순위도 내용 변경 없이 압축합니다. 공개 전환 전에 현재 공개본을 삭제하지 않습니다. 과거 분석 정리 범위는 기존 정책을 유지하고 회원·게시물·사진은 건드리지 않습니다.
- 게시 실패 시 단계와 확인된 Redis 원인을 함께 전달합니다. 용량 초과·요청량 제한·전송량 제한·인증 실패를 구분합니다. 제공자가 원인을 주지 않으면 미확인으로 표시하며 비밀번호나 토큰을 출력하지 않습니다. 용량 초과 오류를 자동으로 반복 재시도하지 않습니다.
- 화면 재조회가 실패 직후의 오류 안내를 지워버리지 않도록 했습니다.

## 검증과 범위

자동 테스트 495개 통과, 배포 JavaScript 문법 검사 및 ZIP 무결성 검사 통과. 기존 JSON의 압축·복원 일치, 같은 키에서 저장 공간 축소, 회원 키 보존, 재수집 없는 게시, 키워드 재사용, 용량 오류 표시를 검증했습니다. 독립 코드 리뷰에서 읽기/복원/게시 데이터 손실 문제는 발견되지 않았습니다.

운영 Redis에 직접 접속하거나 실제 게시를 실행하지 않았습니다. 따라서 운영 서버 성공 확인은 배포 후 게시 결과로 해야 합니다. 이 핫픽스는 확인된 중복 저장과 분석 저장 크기를 줄입니다. 다른 서비스의 데이터나 구버전 게시물을 임의로 삭제하지 않습니다. 압축 데이터가 생긴 이후 31.40 이하 코드로 되돌리면 압축 형식을 읽지 못하므로, 롤백이 필요할 때도 이번 버전의 저장소 읽기 호환 코드는 유지해야 합니다.
