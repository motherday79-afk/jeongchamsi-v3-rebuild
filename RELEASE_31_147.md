# 정참 시선 / JCS SPREAD · 31.147

검색창에 정치인 이름 또는 정당 이름을 입력하면 인물 요약과 상세보기 링크, 언론사 이름 목록, 언론사별 보도 대상과 최초·후속 보도 흐름을 표시합니다. 특정 인물 전용 화면이 아니라 등록된 정치인 공통 기능입니다.

- 최신순: 최근 7일 / 누적순: 최근 30일. 한국 시간으로 실제 분석 기간을 표시합니다.
- 언론사 선택 후 보도 대상 정치인을 누르면 해당 정치인의 검색으로 이어집니다. 기간을 바꿀 때 선택한 언론사와 인물을 유지합니다.
- 동일 기사 중복은 언론사·제목과 확인 가능한 URL 식별값으로 제거합니다. 정당 전체 건수에서는 여러 정치인에게 수집된 같은 기사를 한 번만 계산합니다.
- 최초 보도는 정참시 수집 기사 중 발행 시각을 기준으로 합니다. 동일 시각은 공동, 정확한 시각이 불명확하면 최초 표시는 생략합니다. 세계 최초나 단독 보도, 정치적 지지 의도를 뜻하지 않습니다.
- 구체적인 제목 공통어를 통해 이슈를 연결합니다. 후속 보도는 같은 언론사가 동일 이슈에 대해 시간차를 두고 낸 추가 기사입니다. 다른 언론사의 이후 보도는 앞선 기사를 인용했다는 뜻이 아닙니다.

## 적용

31.146을 사용 중이면 PATCH의 경로를 그대로 덮어쓴 뒤 기존 배포 방식으로 배포합니다. 전체 파일이 필요하면 FULL ZIP을 사용합니다. 추가 환경변수는 없습니다. 이번 대화 환경에서는 운영 서버에 배포하지 않았습니다.

## 기존 데이터와 보존 범위

기존 압축 저장본에는 14일치 전체 제목, 대표 기사 일부, 언론사별 합계가 남아 있습니다. 제목·발행 시각이 없는 합계를 최초·후속 보도로 환산하지 않습니다. 이 버전은 지금 저장된 기사로 바로 분석하고, 신규 수집부터 기존 제목 저장 공간을 30일로 확장합니다. 별도의 기사 본문을 수집하지 않습니다. 기존에 사라진 기사별 정보는 이 업데이트로 복원되지 않습니다.

분석 화면의 건수는 해당 기간에 기사별 정보로 확인 가능한 범위입니다. 따라서 이전 상세페이지의 집계일 기준 언론사별 합계와 다를 수 있습니다. 수집되지 않은 언론사나 기사를 임의로 추가하지 않습니다.

## 성능 및 검증

공개 버전의 압축 입력만 읽어 언론사 공통 인덱스를 만들며, 정치인별 상세 진단을 검색 중에 생성하지 않습니다. 정상 공개 전환 시 인덱스를 미리 생성하고 기존 버전은 첫 요청에서 생성합니다. 동시 최초 요청은 병합하고, 서버 메모리와 압축 저장 인덱스를 재사용합니다. 저장 인덱스는 4 MiB 이하만 저장하고 24시간 후 만료되며 다시 생성할 수 있습니다. 개별 정치인의 공개 갱신은 인덱스를 무효화합니다.

검증: 새 테스트 16개와 기존 관련 테스트 41개, 총 57개 통과. 새 테스트: tests/media-spread*.test.js. 기존 저장·공개·순위 관련 테스트도 별도로 실행했습니다. 모바일 CSS는 3열 언론사 목록과 세로 분석 구성입니다. 실제 운영 DB 응답과 PC/모바일 브라우저 화면은 이 작업 환경에서 확인하지 못했습니다.

합성 데이터 성능 확인: 정치인 543명, 기사 32,580건에서 공통 인덱스 생성 1,135ms, 검색 집계 27ms, 압축 인덱스 398,045바이트. 운영망 응답 속도를 측정한 값은 아닙니다.
