# JCS 0.0.30 정치 인텔리전스 콘텐츠 완성 패치

## 적용 기준

- 기준 버전: **JCS 0.0.29.1 Redis latest-only hotfix 적용 상태**
- 적용 방법: ZIP의 디렉터리 구조를 유지한 채 프로젝트 루트에 덮어쓴다.
- 배포 후 브라우저 캐시는 `0.0.30` 키로 갱신된다.
- 기존 공개 스냅샷은 유지된다. 최신 뉴스·검색 원천값까지 다시 반영하려면 배포 후 관리자에서 수집 완료 후 공개 적용을 1회 실행한다.

## A. 정치 진단 10개

- 모든 542명에게 01~10 진단을 생성한다.
- 핵심 사건 → 정치적 의미 → 현재 위치 → 변화 원인 → 과거·현재 연결 → 근거/비교 → 기회/위험 순으로 구성한다.
- 임의 난수와 이름·ID 해시 점수를 제거하고 뉴스 프레임, 공식 직책·선수·위원회·지역, 실제 세대 구조 및 동급 정치인 구조값만 사용한다.

## B. 정치 처방 10개

- 진단 10개가 모두 끝난 뒤 처방 10개를 별도 PART로 노출한다.
- 각 처방은 연결 진단, 목표, 전략 판단, 행동 3개, 타깃, 메시지, 채널, 구체 시점, 예상 변화와 추적 지표를 포함한다.
- 즉시 3개, 30일 3개, 90일 2개, 중장기 2개로 우선순위를 배정한다.

## C. 뉴스 헤드라인 분석

- 제목 정규화와 토큰 유사도로 중복 사건을 제거한다.
- 매체당 최대 2개, 전체 대표 기사 최대 10개만 사용한다.
- 의제, 긍정·중립·부정 프레임, 정치인 주도/외부 서사, 대표 사건, 정치 자산/부담을 분리한다.
- 동률 의제는 최신 대표 사건을 우선한다.

## D. 과거와 현재 연결

- 공식 선수, 현재 직책, 정당, 지역구, 위원회와 현재 대표 사건을 연결해 일시 노출인지 정치 자산 축적인지 해석한다.

## E. 검색 데이터

- PC·모바일 검색량은 `supportingData`의 보조 근거로만 사용한다.
- 사건·정치 의미보다 검색량이 진단의 중심이 되지 않는다.

## F. 542명 충전 결과

- 대상: 542명
- 진단/처방 미완성: 0명
- 금지된 임시 문구: 0명
- 점수 패턴: 14개 구조 패턴
- 헤드라인 조합: 498개
- 이름을 제거한 전체 진단 서술 조합: 510개

## G. 중복 방지

- 같은 정치 근거에는 같은 값을 생성하며, 이름이나 ID만 바꾼 난수 차이를 만들지 않는다.
- 직책·지역·정당·경력 구조를 반영해 이름만 바꾼 동일 보고서 반복을 줄였다.
- 한국어 조사 생성기를 적용해 `민생·경제을`, `20대 여성가` 같은 문구 오류를 제거했다.

## H. 상세페이지 권한

- 비로그인: 01, 07, 09와 현재 현상만 응답/표시
- 일반회원: 01, 02, 03, 05, 07, 09와 변화·비교·해석까지 표시
- 관리자: 진단 01~10 → 처방 01~10 → 실행 우선순위 표시
- 관리자 전용 해석 문구는 페이지당 한 번만 표시한다.

## I. 비교하기 권한

- 비로그인·일반회원: 최대 2명
- 관리자: 최소 2명, 최대 4명
- 상세와 비교는 동일한 서버 투영 데이터와 동일 지표를 사용한다.

## J. 관리자 2·3·4인 비교

- 2·3·4명 모두 선택 정치인별 고정 프로필 헤더와 10개 비교 매트릭스를 렌더한다.
- 기준 정치인을 변경할 수 있다.
- 선택된 상대마다 공세, 방어, 회피, 단기 역전 가능 영역을 별도 카드로 제공한다.

## K. API 누출 방지

- 비로그인 응답에는 회원 상세 근거와 관리자 처방이 포함되지 않는다.
- 일반회원 응답에는 관리자 기회·위험·전략·처방이 포함되지 않는다.
- 중첩 객체도 필드 허용목록으로 다시 투영한다.
- 542명 감사 결과 권한 누출: 0건

## L. PC·모바일 확인

- 상세 3개 권한과 비교 3개 권한의 실제 렌더 HTML, 3·4인 매트릭스, 사진 헤더, 검색·추가·삭제 경로를 자동 테스트했다.
- 모바일 단일열 및 관리자 비교 가로 스크롤 CSS를 검증했다.
- 이 작업 환경의 클라우드 브라우저가 로컬 파일 URL을 차단하여 실 브라우저 캡처 파일은 생성하지 못했다. 배포 URL에서의 최종 시각 확인은 남아 있다.

## M. 변경 파일

- 분석: `lib/intelligence-headlines.js`, `lib/intelligence-analysis.js`, `lib/intelligence-diagnostics.js`, `lib/intelligence-validation.js`
- 권한/API: `lib/intelligence-access.js`, `api/gateway.js`
- 화면: `src/views/politicians.js`, `src/views/politician-compare.js`, `css/pages.css`
- 버전/캐시: `package.json`, `package-lock.json`, `index.html`, `src/app.js`
- 검증: `tests/intelligence-headlines.test.js`, `tests/intelligence-v2.test.js`, `tests/politician-diagnostics-api.test.js`, `tests/politician-diagnostics-view.test.js`, `tests/politician-diagnostics-compare.test.js`, `tests/politicians.test.js`

## N. 테스트 결과

- `npm test`: **225 통과 / 0 실패**
- 상세 권한, 비교 권한, 서버 필드 차단, 542명 완전 생성, 중복 기사 최대 10개, 한국어 문구, 릴리스 캐시 키를 포함한다.

## O. 패치 파일

- `JCS_0_0_30_POLITICAL_INTELLIGENCE_CONTENT_PATCH.zip`
- GitHub 등록·배포는 사용자 진행 범위이며 이 작업에서는 수행하지 않았다.

## 실제 데이터 관련 메모

- 새 유료 API나 외부 수집기는 추가하지 않았다.
- 뉴스가 없는 경우 임의 숫자를 만들지 않고 공식 정치 구조를 근거로 진단한다.
- 최신 검색·뉴스 수치를 갱신하려면 관리자 수집·공개 적용이 필요하다.
