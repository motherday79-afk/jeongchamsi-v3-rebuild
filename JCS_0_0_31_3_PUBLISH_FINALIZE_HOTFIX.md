# JCS 0.0.31.3 전체 게시 최종 전환 핫픽스

## 실제 재현 결과

- 관리자에서 `전체 게시`를 직접 실행했습니다.
- 게시 배치는 542/542까지 처리됐지만 공개 스냅샷은 `jcs-20260903180356`에 머물렀습니다.
- 수집·승인 스냅샷 `jcs-20260904100022`는 정상이며 다시 수집할 필요가 없습니다.

## 직접 원인

현재 공개 포인터가 가리키는 이전 스냅샷의 버전 메타데이터가 이미 삭제된 상태였습니다. 마지막 공개 전환 과정은 존재하지 않는 이전 버전을 반드시 `archived`로 바꾸려 했고, `ANALYSIS_VERSION_NOT_FOUND`가 발생하면서 새 공개 포인터로 전환하지 못했습니다.

오래된 draft와 failed 스냅샷도 여러 개 남아 있어 Redis 저장 공간을 계속 점유하고 있었습니다.

## 수정 내용

1. 게시 시작 직전에 현재 승인 스냅샷과 기존 공개 스냅샷만 보호하고 나머지 분석 스냅샷·이력을 정리합니다.
2. 이전 공개 버전 메타데이터가 실제로 존재할 때만 archived 처리를 수행합니다.
3. 이전 메타데이터가 없어도 새 공개 스냅샷 전환을 계속합니다.
4. 공개 포인터 전환이 끝난 뒤의 정리 오류는 이미 성공한 게시를 실패로 되돌리지 않습니다.
5. 공개 전환이 실제로 실패하면 게시 작업에 최종 오류 코드를 저장해 관리자 화면 새로고침 후에도 표시합니다.
6. 542건 처리만 끝나고 최종 검증이 실패한 경우 완료로 오인하지 않도록 실행기를 수정했습니다.

## 누적 포함 범위

이 ZIP은 운영 환경에서 이전 용량 패치가 일부 누락된 경우까지 복구할 수 있도록 다음 내용을 누적으로 포함합니다.

- 0.0.31.1 Redis 저장량 축소 및 최신 스냅샷 유지
- 0.0.31.2 일부 수집 오류 시 이번 수집의 성공 데이터만 게시
- 0.0.31.3 게시 최종 전환 복구

## 적용 후 순서

1. ZIP을 저장소 루트에 덮어씁니다.
2. GitHub 반영 후 Vercel 배포 완료를 확인합니다.
3. 관리자 페이지를 강력 새로고침합니다.
4. 수집 스냅샷이 `jcs-20260904100022`, 상태가 `approved`, 검증이 `통과`인지 확인합니다.
5. 다시 수집하지 말고 `전체 게시`만 한 번 실행합니다.
6. 현재 공개 스냅샷도 `jcs-20260904100022`로 바뀌었는지 확인합니다.

정치인 사진과 이번 핫픽스와 관계없는 자산은 포함하지 않았습니다.
