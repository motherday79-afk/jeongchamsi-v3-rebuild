# JCS 0.0.31.31 적용 안내

## 적용 기능

- 메인 `세대별 대통령`을 `세대별` / `대통령`의 의도된 2줄로 고정
- 메인 내 참여 카드의 바깥 박스 호버 제거
- 마이페이지 버튼과 대표·전시 배지 4칸에만 개별 이동·호버 적용
- 메인 네비게이션과 같은 2px 상승·그림자 규칙을 클릭 가능한 UI에 공통 적용
- 메인 사이드 배너 권장 비율을 640 × 350으로 변경
- 정치인과 게시글 즐겨찾기 추가 및 실제 존재하는 대상만 서버 저장
- 마이페이지에 내가 쓴 게시글, 획득한 배지, 획득 가능한 배지, 즐겨찾기한 게시글, 즐겨찾기한 정치인 표시
- 풋터와 전체 메뉴에 1:1 문의하기 추가
- 공개 문의는 누구나 열람, 비공개 문의는 작성자와 관리자만 열람
- 문의 상세에서 관리자 답변 저장 및 노출

## 배포 후 해야 할 일

1. ZIP을 압축 해제합니다.
2. 압축을 푼 폴더 안의 파일·폴더를 현재 GitHub 저장소 최상단에 업로드합니다.
3. 같은 이름의 파일은 교체하고, 기존의 다른 파일은 삭제하지 않습니다.
4. GitHub Actions가 자동으로 시작되지 않으면 해당 워크플로의 `Run workflow`를 누릅니다.
5. 배포 완료 후 브라우저에서 강력 새로고침합니다.

이번 변경은 코드 배포이므로 관리자 데이터 수집·검수·게시를 다시 실행할 필요가 없습니다. 새로운 환경변수도 필요하지 않습니다. 즐겨찾기와 문의 Redis 키는 최초 사용 시 자동 생성됩니다.

## 쪽지 기능 판단

회원 간 쪽지 본문 전체를 현재 Redis에 계속 누적하면 용량과 조회 비용이 커질 수 있습니다. 이번 버전에는 쪽지를 넣지 않았습니다. 추후 구현한다면 메시지 본문은 별도 영구 DB에 저장하고 Redis에는 안 읽은 개수와 최근 상태만 두는 구성이 안전합니다.
