# 0.0.31.340 — 매일 낮 12시 NOW Rank 자동 갱신

- 예약: 매일 03:00 UTC = 한국 시간 낮 12시. Vercel Hobby는 해당 시간대 내 실행하므로 분 단위 정시 실행을 보장하지 않음. 자료 수집 시작 예약이며 공개는 수집·검증 완료 후.
- 기존 수집/분석/원자적 게시 기능을 서버 작업으로 연결. 관리자 브라우저가 없어도 10명 단위 수집 및 게시 단계를 이어감.
- 같은 날짜의 중복 시작과 같은 단계의 중복 처리 차단. 예약 작업과 수동 전체 수집/게시 변경은 공통 Redis 잠금으로 직렬화.
- 자동 작업은 필수 순위 자료(Naver 검색량/Google 뉴스)의 소스 오류 및 일부 뉴스 기간 수집 실패를 수집 실패로 처리. 과거 뉴스 병합 전에 최신 수집 결과를 확인. 실패 인물을 한 번 다시 수집한 뒤에도 누락이 있으면 게시하지 않음.
- 단계 예외 최대 3회, 서버 간 다음 단계 호출 최대 3회. 실패 시 공개 순위 유지, 관리자 데이터수집·게시 탭에 실행 상태 및 오류 표시. 작업 상세는 최근 30일 보관.
- 각 호출 최대 60초, 잠금 90초, 한 실행 최대 6시간/2,000단계. 플랫폼 강제 종료 시 관리자 화면에 지연 상태 표시. 같은 날 예약 작업 재실행은 중단 단계부터 이어짐. 다음 날 미완료 작업은 만료 처리.
- CRON_SECRET 인증 후에만 저장소 접근. Production만 실행. 고정된 jeongchamsi-v3-rebuild.vercel.app 주소로만 다음 단계를 호출하고 리디렉션 금지. 비밀값은 코드/브라우저/로그에 노출하지 않음.
- NOW Rank 분야별 순위에 마지막 공개 갱신 시각 표시. 기존 17시 인적 여론조사 예약과 광산 BGM은 유지.

검증: 예약/중복/실패/수동 충돌/인증/서버 연속 호출/필수 소스 누락/표시 관련 테스트, 기존 API·수집·게시 화면 테스트 총 32개 통과. 잠금 충돌 시에도 작업을 재전달하는 회귀 검사 포함. 기존 intelligence-admin-view.test.js 8개 실패는 이전 HEAD 파일로 동일하게 재현됨(기본 탭 및 오래된 버전 기대값).
운영 전체 재수집을 테스트 목적으로 즉시 실행하지 않음. 배포 및 예약 등록을 확인하고 다음 예약 시점부터 실행.
