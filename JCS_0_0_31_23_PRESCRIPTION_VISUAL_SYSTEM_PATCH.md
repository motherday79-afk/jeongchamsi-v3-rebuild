# JCS 0.0.31.23 — 처방 01~10 시각 엔진

## 적용 기준

- GitHub 재체크아웃·초기화·pull 없이 기존 누적 작업 디렉터리와 31.22 결과물 위에서 이어서 작업했습니다.
- 진단은 기존 골드+네이비 시각 체계를 유지하고, 처방은 같은 역할과 빈도로 왕청색+네이비를 사용합니다.
- 파란 배경 박스를 반복하지 않습니다. 흰색 리포트를 기본으로 번호·핵심 판단·주요 그래프 신호에만 왕청색을 사용합니다.
- 관측값이 없는 선택 데이터에는 `연결 전`, `관측 없음`, `데이터 없음` 같은 대체 문구나 가짜 값을 넣지 않고 해당 선택 요소를 렌더링하지 않습니다.

## 처방 엔진

1. 모든 등록 정치인에게 동일한 데이터 계약으로 10개 진단과 연결된 10개 처방을 생성합니다.
2. 처방 01~10은 복제 카드가 아니라 각각 다른 실행 도구로 표시합니다.
   - 01 브랜드 메시지 피라미드
   - 02 세대·성별 타깃 매트릭스
   - 03 지역구 4단계 메시지 플레이북
   - 04 코어·유동·이탈 확장 흐름
   - 05 경쟁자 대응 매트릭스
   - 06 위기 대응 타임라인
   - 07 언론·온라인 확산 흐름
   - 08 선거·캠페인 자원 보드
   - 09 정치 활동→미디어 전환 흐름
   - 10 JCS 종합 실행 우선순위 보드
3. 09는 `정치 활동·미디어 전환 처방`, 10은 `JCS 종합 실행 처방`으로 진단 09·10의 변경 방향을 그대로 반영합니다.
4. 비교하기의 관리자 처방에도 같은 09·10 명칭과 처방 데이터가 적용됩니다.

## 노출과 속도

- 비로그인: 기존 진단 01·07·09만 제공하며 관리자 처방 버튼·payload·결과를 보내지 않습니다.
- 일반회원: 기존 진단 01·02·03·05·07·09만 제공하며 관리자 처방 버튼·payload·결과를 보내지 않습니다.
- 관리자: 진단 01~10 뒤에 `10개 처방 전체보기 ＋`를 표시합니다.
- 처방 10개는 첫 화면에서 만들지 않습니다. 관리자가 전체보기를 처음 누를 때 한 번만 렌더링하고, 이후에는 펼치기·접기만 수행하여 중복 생성하지 않습니다.
- 데스크톱·태블릿·모바일에서 10개 처방 도구가 각각 2열·1열 또는 4단계·2단계·1단계로 안전하게 재배치됩니다.

## 배포 후 반드시 할 일

1. ZIP 안의 파일을 현재 저장소 루트에 덮어써서 배포합니다.
2. 브라우저를 강력 새로고침하고 관리자 화면에서 `JCS_0_0_31_23`을 확인합니다.
3. 관리자에서 `데이터 수집 → 검수 승인 → 게시`를 한 번 실행합니다.

3번이 필요한 이유는 처방 09·10의 데이터 계약과 모든 정치인의 처방 결과를 새 버전으로 다시 생성·승인·게시해야 운영 상세페이지와 비교하기에 반영되기 때문입니다.

## 검증

- 전체 자동 테스트: 383 통과 / 0 실패
- 전체 등록 정치인 542명: 각 10개 진단·10개 처방 결정론적 생성 확인
- 관리자 상세: 처방 10종 레이아웃, 최초 1회 지연 렌더링, 접기·재열기 확인
- 비로그인·일반회원: 관리자 처방 버튼·payload·결과 비노출 확인
- 선택 데이터 누락 시 금지 placeholder 및 빈 선택 요소 미출력 확인
- 관리자 비교: 변경된 처방 09·10 명칭·의미 동기화 확인
- JavaScript 구문, Git 공백, 전체 자동 테스트, ZIP CRC·항목·원본 바이트 검증
