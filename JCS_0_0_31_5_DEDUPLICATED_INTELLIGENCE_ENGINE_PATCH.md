# JCS 0.0.31.5 · 항목별 인텔리전스 중복 제거 핫픽스

## 적용 목적

정치인 상세페이지와 비교하기에서 1~10번 진단 및 처방에 같은 문장과 같은 근거가 반복되던 문제를 수정한다. 수집된 뉴스·검색·공식 프로필·연령×성별 자료를 항목별 용도에 맞게 분배하고, 게시 전 중복 검수를 강화한다.

## 주요 변경

- 01~10번의 현재 위치, 정치적 의미, 정참시 해석, 기회 요인, 위험 요인을 항목별 문장으로 분리했다.
- 하나의 근거가 여러 항목에 무제한 재사용되지 않도록 항목 라우팅과 사용 횟수를 제한했다.
- 핵심 사건과 공통 근거는 01번 진단에 집중하고, 처방의 `진단 근거`에는 연결된 실제 근거 문장을 표시한다.
- 10개 처방의 예상 변화와 추적 지표를 항목별로 각각 다르게 생성한다.
- 다음 상태를 게시 전 오류로 차단한다.
  - 현재 위치·정치적 의미·정참시 해석의 복사
  - 동일 근거의 과도한 재사용
  - 예상 변화·추적 지표의 반복
  - 금지된 범용 문구
  - `[object Object]` 화면 출력
- 02 세대·성별 진단 그래프에서 연령별 남성·여성 값을 각각 표시한다.
- 관리자 정치 유형 카드의 근거 객체가 `[object Object]`로 출력되는 문제를 수정했다.

## 적용 순서

1. ZIP을 프로젝트 루트에 덮어쓴다.
2. 배포한다.
3. 관리자에서 새 전체 수집을 실행한다.
4. 수집 완료 후 분석·검수 상태를 확인한다.
5. 게시 가능 상태에서 전체 게시를 실행한다.

기존에 이미 게시된 스냅샷은 자동으로 새 문장으로 바뀌지 않으므로, 배포 후 새로 수집하고 게시해야 한다.

## 검증 결과

- 전체 자동 테스트: 276개 통과, 실패 0개
- 542명 전원: 진단 10개·처방 10개 생성 및 게시 계약 통과
- 동일 정치인의 상세페이지·비교하기 데이터 일치 검증 통과
- Redis 최신 스냅샷 단일 보관 및 25명 단위 배치 검증 통과
- 이미지 파일 포함: 0개

