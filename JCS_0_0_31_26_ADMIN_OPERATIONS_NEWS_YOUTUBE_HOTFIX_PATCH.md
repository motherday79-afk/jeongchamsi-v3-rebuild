# JCS 0.0.31.26 — 관리자 운영 · 뉴스 기간 집계 · YouTube 상세 핫픽스

## 작업 원칙

- GitHub clone, pull, reset, checkout, init 없이 현재 누적 작업 디렉터리에서만 이어서 작업했다.
- 기존 정치인 상세페이지의 승인된 진단·처방 디자인과 비로그인·일반회원·관리자 공개 범위는 유지했다.
- 비밀번호 원문·해시, API 키, 세션 비밀, 사진 바이너리는 관리자 응답이나 ZIP에 포함하지 않는다.

## 관리자 회원관리

- 이름, 닉네임, 이메일, 전화번호, 출생연도, 지역, 선호 정당, 아이디, 상태, 권한을 관리자가 수정할 수 있다.
- 현재 비밀번호는 조회할 수 없고 8자 이상의 임시 비밀번호로만 초기화한다.
- 임시 비밀번호는 scrypt 해시만 저장하고, 초기화 즉시 기존 세션을 무효화하며 다음 로그인에서 새 비밀번호 변경을 요구한다.
- 회원은 삭제 대신 이용정지·복구로 관리하며 자기 자신 또는 마지막 정상 관리자 계정의 권한을 제거할 수 없다.
- 기존 배지 관리와 회원 활동 데이터는 유지하며 아이디를 변경할 때 활동·작성자 식별자도 함께 이관한다.

## 관리자 정치인정보 관리

- 전체 정치인을 이름·정당·지역·ID로 검색해 사진, PAST RISK SIGNALS, 뉴스 오탐 제외값, 공식 YouTube 채널을 수정할 수 있다.
- PAST RISK는 `#태그 | 기사 제목 | HTTPS 기사 주소 | YYYY-MM-DD` 형식으로 최대 5개를 영구 저장하고 다음 수집에도 적용한다.
- 사진은 JPG·PNG·WebP와 실제 파일 시그니처를 서버에서 다시 검사하며 최대 1MB로 제한한다.
- 사진 선택 즉시 업로드 전 미리보기를 표시하고 기존 사진은 확인 후 교체한다.
- 사진 파일은 Vercel Blob public store에 보관하고 Redis에는 공개 URL·경로·크기·형식·초점·수정자·시각만 저장한다.
- 사진 교체 시 이전 URL·경로와 새 URL·경로를 관리자 감사기록에 남긴다.
- 데이터 완성도에서 사진·PAST RISK·YouTube 누락 수를 집계한다.
- 회원 수정, 비밀번호 초기화, 사진·위험신호·뉴스 제외 수정 등 관리자 작업은 최근 300건까지 감사기록으로 보관한다.

## 한 명 수집·검수·게시

- 정치인정보 탭에서 한 명만 새로 수집하고, 검수 승인한 뒤 그 정치인의 공개 데이터만 게시할 수 있다.
- 수집 또는 승인만으로 현재 공개 상세페이지를 바꾸지 않는다.
- 한 명 게시에서도 기존 전체 NOW 순위는 유지하며 다음 전체 게시에서 새 전체 순위로 대체한다.

## Google 뉴스 기간 집계 수정

기존의 기간 없는 관련도 검색은 Google 일반 검색 결과와 다르게 오래된 기사 위주로 반환될 수 있었다. 이번 버전은 정치인별로 `when:1d`, `when:7d`, `when:30d` Google News RSS를 순차 수집한다.

- 실제 발행일을 기준으로 24H·7D·30D를 다시 계산한다.
- 제목과 설명에 정치인 이름이 없는 결과를 제거한다.
- 정규화한 `제목 + 매체` fingerprint로 세 기간의 중복기사를 제거한다.
- 관리자가 저장한 오탐 기사 fingerprint 또는 `source:기사제목`을 다음 수집부터 제외한다.
- 화면에 `Google News RSS · 최근 1일/7일/30일` 수집 기준과 수집 시각을 표시한다.
- 세 기간은 동일 기사 원장을 사용하므로 `24H ≤ 7D ≤ 30D` 관계를 유지한다.

배포만으로 기존 공개 스냅샷의 고민정 등 뉴스 건수가 자동 변경되지는 않는다. 해당 정치인의 `새로 수집 → 검수 승인 → 게시` 또는 전체 수집·승인·게시를 완료해야 새 기준이 공개 화면에 반영된다.

## YouTube 상세페이지

- 관리자가 정치인 검색 결과에서 채널 URL, `@handle`, UC 채널 ID를 직접 저장할 수 있다.
- 관리자 수동값은 이후 자동검색이 덮어쓰지 않는다.
- 07 언론 영역에는 등록·수집된 공식 채널이 있을 때만 YouTube 블록을 표시한다.
- 24H·7D·30D 각각 업로드 수와 그 기간에 업로드된 영상의 현재 평균 조회수를 계산한다.
- 최근 영상은 처음 3개만 표시하고 `최근 영상 더보기`를 누르면 같은 위치에서 아래로 펼친다.
- 펼침 한도는 기존 권한 정책대로 비로그인 3개, 일반회원 5개, 관리자 10개다.
- 구독자, 좋아요, 댓글, 감성 분석, 영상 업로드, YouTube 로그인 기능은 추가하지 않았다.

채널 주소를 저장한 것만으로 과거 공개 스냅샷에 영상이 생기지는 않는다. 채널 저장 후 해당 정치인을 다시 수집·승인·게시해야 07번에 표시된다.

## 배포 전에 확인할 환경 설정

### 이미 설정한 YouTube API

- `YOUTUBE_DATA_API_KEY`: Google Cloud에서 YouTube Data API v3로 제한한 서버용 API 키
- OAuth 동의 화면과 YouTube 계정 로그인은 필요하지 않다.

### 사진 업로드에 추가로 필요한 Vercel Blob

1. Vercel 프로젝트의 Storage에서 Blob store를 만들고 현재 정참시 프로젝트에 연결한다.
2. 연결 결과로 `BLOB_READ_WRITE_TOKEN`이 Production 환경에 제공되는지 확인한다.
3. 새 배포 후 관리자 정치인정보에서 1MB 이하 테스트 사진을 선택해 미리보기와 저장을 확인한다.

Blob 연결 전에도 회원관리, YouTube 수동등록, PAST RISK, 뉴스 수집 기능은 동작하지만 정치인 사진 업로드는 실패한다.

## 배포 후 운영 순서

1. ZIP을 현재 31.25 누적 소스 위에 덮어쓰고 의존성을 설치한 뒤 배포한다.
2. 관리자 화면에서 버전 `JCS_0_0_31_26`과 API 연결 상태를 확인한다.
3. 브라우저를 강력 새로고침한다.
4. 정치인정보에서 잘못 연결된 YouTube 주소, 사진, PAST RISK, 뉴스 오탐 제외값을 교정한다.
5. 소수 정치인은 `새로 수집 → 검수 승인 → 게시`, 전체 반영은 `전체 수집 → 검수 승인 → 전체 게시` 순서로 진행한다.
6. 상세페이지 07번에서 새 뉴스 기준, 세 기간 값, YouTube 평균 조회수와 최근 영상 펼침을 확인한다.

## 검증 결과

- 전체 자동 테스트 427개 통과
- JavaScript 구문 검사 통과
- Git 공백 오류 검사 통과
- CSS 중괄호 균형 검사 통과
- 비밀번호·API 키·세션 비밀의 패키지 포함 여부 검사 통과
- ZIP CRC 검사와 ZIP 각 파일의 원본 바이트 일치 검증 통과
