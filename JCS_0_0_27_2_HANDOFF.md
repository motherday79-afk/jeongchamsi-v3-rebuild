# JCS 0.0.27.2 적용 안내

이 ZIP은 기존 `JCS_0_0_27.1` 패치를 대체하는 전체 보완 패치입니다. 이전 ZIP과 함께 올리지 말고 이 파일 하나만 프로젝트 루트에 덮어쓰면 됩니다.

## 이번 긴급 수정

- 메인 서비스 카드에서 임의로 추가됐던 하단 설명 문구를 전부 제거했습니다.
- 상위 6개 메뉴는 그대로 유지하고, `전체 서비스`를 눌렀을 때 추가 7개만 아래에 표시합니다.
- 메인 검색 자동완성 결과를 가리던 `overflow:hidden`을 실제 최종 로드 CSS에서도 제거했습니다.
- 정치인·정당·지역은 한 글자부터 사진이 포함된 자동완성 목록으로 표시합니다.
- 비교하기는 슬롯마다 반복되던 좁은 검색창을 제거하고, 상단 전체 폭 검색창 하나에서 정치인을 계속 추가하도록 변경했습니다.
- PC에서는 최대 5개 비교 슬롯을 한 행에 유지하고, 모바일에서는 한 열로 정리해 검색창과 카드가 겹치지 않게 했습니다.
- 캐시 키와 릴리스 표기를 `0.0.27.2`로 변경했습니다.

## GitHub 적용

1. ZIP의 폴더 구조를 유지한 채 저장소 최상위에 업로드합니다.
2. GitHub에서 기존 파일 덮어쓰기를 확인하고 커밋합니다.
3. Vercel 배포가 끝나면 PC에서 `Ctrl + Shift + R`, 모바일에서 브라우저 캐시 새로고침을 한 번 실행합니다.

## 배포 후 확인

- 메인 서비스 카드에 메뉴명 외 하단 설명문이 없는지 확인
- 메인 검색창에 `김` 입력 시 사진·이름·정당·지역 목록이 나타나는지 확인
- `/compare`에서 `김` 입력 후 정치인을 연속 선택할 수 있는지 확인
- 관리자 비교 화면에서 2~5명 선택 후 `비교하기`를 눌렀을 때만 결과가 표시되는지 확인

## 검증 결과

- 이번 수정 회귀 테스트: 9/9 통과
- 전체 테스트: 187개 중 186개 통과 예정 범위이며, 기존 사진 개수 검증 1개만 별도 이슈입니다.
- 기존 사진 개수 테스트는 코드 기대값 515개와 실제 폴더 593개의 차이이며 이번 패치에서 사진을 추가·삭제하지 않았습니다.
- JavaScript 문법 및 Git diff 공백 검사를 함께 실행했습니다.
- 작업 공간의 로컬 주소는 보안 정책상 원격 브라우저에서 열 수 없어, 배포 후 위 4개 화면을 최종 확인해야 합니다.
