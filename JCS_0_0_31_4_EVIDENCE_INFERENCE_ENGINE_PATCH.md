# JCS 0.0.31.4 다중 근거 추론·Redis 게시 핫픽스

## 적용 내용

- 기사 제목·설명, 공식 직책·경력·지역, 검색 보조 신호, 비교 정치인을 정치 사실로 구조화합니다.
- 정치 사실을 01~10번 진단 항목별로 따로 배분하고, 항목마다 서로 다른 정치적 판단·기회·위험을 생성합니다.
- 처방의 근거 데이터가 진단 제목 복사가 아니라 실제 기사·공식 구조·수치 사실을 가리키도록 변경했습니다.
- `해당 핵심 이슈는 긍정과 부정 효과가 함께 나타나…` 범용 반복 문구를 제거하고 동일 정치적 의미의 반복을 게시 전 검수에서 차단합니다.
- 기사 원문 제목의 매체 머리말·꼬리말은 핵심 사건 한 곳 외 진단 문장과 근거 문장에 반복하지 않습니다.
- Google 뉴스 RSS 설명은 태그를 제거하고 최대 360자로 제한하며, 정치인별 기사 입력은 최대 10건만 저장합니다.
- Redis 분석 버전 메타데이터에서 최대 수천 개의 기사 URL 목록을 제거하고 기사 총건수만 보관합니다.
- 전체 수집 중 오류가 있어도 성공한 현재 데이터의 자동 검수가 통과하면 승인·게시할 수 있습니다.
- 최신 초안 포인터 조회가 비어도 완료된 수집 작업의 스냅샷을 기준으로 게시 버튼 상태를 복구합니다.
- 기존 게시 최종 전환 보호와 공개 포인터 복구 핫픽스를 그대로 포함합니다.

## 배포 후 관리자 순서

1. `전체 수집`을 실행해 완료 또는 일부 오류 상태까지 진행합니다.
2. 자동 검수 통과를 확인하고 `검수 승인`을 누릅니다.
3. 활성화된 `전체 게시`를 눌러 공개 스냅샷 전환까지 완료합니다.

일부 수집 오류가 있으면 과거 데이터를 되살리지 않고 이번 수집에서 성공한 정치인만 게시합니다.

## 검증 결과

- `npm test`: 273개 통과, 실패 0개
- 542명 전체 진단 10개·처방 10개 생성 검증 통과
- 오세훈 골든 픽스처: 5선 경력, 0.60%p 접전, 1심 벌금·직 상실 위험, 서울시의회 81석 제약, 주택 31만호 정책을 서로 다른 항목에 연결
- Redis 버전 메타데이터 대량 기사 URL 미저장 검증 통과
- 일부 오류 완료 상태 및 완료 스냅샷 포인터 복구 시 게시 버튼 활성화 검증 통과

## 적용 방법

ZIP의 폴더 구조를 유지한 채 프로젝트 루트에 덮어쓴 후 배포합니다. 정치인 사진과 관계없는 자산은 포함하지 않았습니다.
