# JCS SPREAD · 월간 보도와 검색의 위치 · 31.149

## 적용 내용

정치인 검색 결과의 프로필 바로 아래에 월간 보도·검색 비교를 추가했습니다. 기존 언론사 분석은 그 아래에서 이어집니다.

- 선택한 정치인의 현재 위치를 한 문장으로 먼저 보여줍니다.
- 네이버 월간 PC·모바일 검색량과 수집 기준 최근 30일 보도량, 각각의 순위를 나란히 표시합니다.
- 같은 직군의 비교 인물은 옅은 점, 검색한 사람은 퍼플·골드 점과 이름으로 표시합니다.
- 보도량이 비슷하면서 검색량이 더 많은 인물, 검색량이 비슷하면서 보도량이 더 많은 인물, 두 값이 모두 비슷한 인물을 연결합니다. 조건에 맞는 인물이 없으면 해당 추천은 만들지 않습니다.
- 관련 인물의 검색 결과와 기존 두 사람 비교하기로 바로 이동합니다.
- 월간 비교는 아래 언론사 분석의 최신순(7일)·누적순(30일) 및 언론사 선택과 독립적으로 유지됩니다.
- 모바일에서는 설명·수치·차트·관련 인물 순서로 세로 배치합니다.

## 집계 기준

검색량은 네이버 검색광고 API의 월간 키워드 검색 횟수입니다. 검색한 사람 수나 지지율이 아닙니다. 뉴스는 수집 시점 기준 최근 30일 기사 수이며, 네이버의 월간 검색량과 동일한 일별 시계열로 간주하지 않습니다. 각각의 수집 기준일을 표시합니다. 네이버 월간 검색 지표 설명: https://ads.naver.com/help/faq/1406

비교 대상은 같은 등록 직군(국회의원·광역단체장·기초단체장 등)이고 두 출처가 모두 확인된 정치인입니다. 출처끼리의 수집 시각 차이는 48시간 이내, 다른 인물의 각 출처 수집 시각도 검색한 인물과 48시간 이내이며, 30일보다 오래된 관측은 비교하지 않습니다.

보도·검색 각각 경쟁 순위(공동 순위 허용)를 표시합니다. 차트에는 동률을 고려한 순위의 상대 위치를 사용해 극단적으로 큰 한 사람 때문에 나머지 점이 구석에 몰리는 일을 피합니다. 중간선 50에 놓인 지표는 두드러지거나 중간보다 낮다고 해석하지 않고, 그 지표는 중간이며 다른 지표가 중간보다 높은지 낮은지를 정확히 설명합니다. 두 축 모두 50보다 높을 때만 보도와 검색 모두 두드러진다고 설명합니다. 두 지표를 더한 별도 점수는 만들지 않습니다.

비교 인원이 4명 미만이면 확인된 수치만 보여주고 위치를 분류하지 않습니다. 수집되지 않은 값, 범위값(예: 10 미만), 뜻이 불명확한 기존 저장본의 0, 수집 실패나 일부 누락이 있는 보도량 0, 동일 이름으로 구분할 수 없는 등록 인물은 억지로 분류하지 않습니다. 보도량 0은 해당 기간이 빠짐없이 수집된 경우에만 관측값으로 사용합니다. 정당 검색에서 정치인 이름 검색량을 합산해 정당 검색량으로 만들지 않습니다. 정당 결과에서 인물을 선택하면 해당 인물의 비교가 나옵니다.

뉴스 건수에는 원언론사가 미확인인 포털 경유 기사도 포함됩니다. 아래 언론사별 순위는 작성 언론사가 확인된 기사만 사용하므로 두 영역의 기사 수는 다를 수 있습니다. 수집 제한은 집계 근거에 안내합니다.

## 자료 정확성 및 속도

검색 출처의 수집일·월간 수치의 정확/범위 구분을 압축 저장해 유지합니다. 이름에 정확히 일치하는 검색어가 없을 때 관련 키워드의 검색량을 대신 사용하는 경로도 막았습니다. 기존 운영 NOW 순위 계산 방식은 변경하지 않았습니다.

31.148 언론사 인덱스는 버전 검사 후 기존 공개 저장 자료에서 자동 재생성됩니다. 전체 재수집이나 개별 상세 리포트 재호출은 필요하지 않습니다. 매 검색마다 외부 API를 부르지 않으며 기존 공개 캐시를 재사용합니다. 공개되지 않은 관리자 리포트·전략 처방은 응답에 포함하지 않습니다.

합성 자료 543명으로 측정한 추가 지표 추출은 약 3ms, 300명 비교 집계는 100회 평균 약 1ms였습니다. 추가 지표의 압축 저장 크기는 약 11KB였습니다. 로컬 계산 측정이며 운영 서버의 응답시간을 보장하는 수치가 아닙니다.

## 적용

관련 자동 검증 104개를 통과했습니다. 월간 집계·동률과 중간 위치·미확인 값·수집 실패 후 재저장·기간 분리·추천 조건·공개 캐시·기존 수집/상세/언론사 기능 및 화면 출력과 링크를 검증했습니다. 수정된 JavaScript 문법, 상대 경로 의존성, ZIP 무결성, 31.148에 PATCH를 적용한 결과와 FULL ZIP의 바이트 일치도 확인했습니다.

31.148을 사용 중이면 PATCH ZIP의 경로를 유지해서 덮어쓴 후 기존 방식으로 배포합니다. 전체 교체는 FULL ZIP을 사용합니다. 운영 데이터가 충분한 정치인을 검색해 새 영역을 확인합니다. 자료가 비교 요건을 충족하지 못하면 기존 프로필·언론사 결과는 유지되고 새 분류를 임의로 채우지 않습니다.

이 작업에서 운영 서버 배포와 실제 DB 검증은 수행하지 않았습니다. 실행 가능한 브라우저가 없어 실제 브라우저/단말의 화면·터치 동작 검증도 수행하지 못했습니다. 반응형 CSS와 출력 HTML·SVG, 라우팅 및 데이터 동작을 코드 수준에서 검증했습니다.
