# JCS 0.0.31.35 적용 안내

## 적용 순서

1. 현재 운영 저장소를 초기화하거나 다시 체크아웃하지 않습니다.
2. ZIP을 풀어 기존 프로젝트의 같은 경로에 파일을 반영합니다.
3. 아래 구형 스타일 파일이 운영 저장소에 남아 있으면 삭제합니다.
   - `css/hotfix-31-11.css`
   - `css/hotfix-31-34-main-detail-recovery.css`
4. 변경 사항을 커밋하고 배포합니다.
5. GitHub Actions가 자동 실행되지 않았을 때만 `Run workflow`를 누릅니다.
6. 배포 후 관리자 화면에서 `JCS_0_0_31_35`가 표시되는지 확인합니다.

## 유지되는 환경설정

- 정치인 사진과 메인 배너 업로드에는 Vercel Blob 연결이 필요합니다.
- 메인 배너 권장 크기는 640 × 450px이며 최대 용량은 2MB입니다.
- 정치인 사진은 JPG·PNG·WEBP, 최대 1MB입니다.
- YouTube는 기존에 연결한 API 환경변수를 그대로 사용합니다.

## 주요 변경

- 정치인 상세는 승인된 흰색 축약 프로필과 최신 진단 디자인만 사용합니다.
- 04 핵심 지지층은 서로 겹치지 않는 독립지수 도형 3개만 렌더링합니다.
- 세대별 대통령은 가입 출생연도에 따른 본인 연령대에서만 1회 투표합니다.
- 정치인 등록 요청과 비공개 파트너스 신청·관리 흐름을 추가했습니다.
- 신규 회원 필수정보, 전화번호 형식, 시·도/시·군·구 연동을 적용했습니다.
- 메인 내비게이션을 텍스트 중심 최소형으로 바꾸고 MY 영역을 축약했습니다.
- 관리자 작업기록은 펼칠 때만 서버에서 불러옵니다.

## 검증 결과

- Node 자동 테스트: 449개 전체 통과
- JavaScript 구문 검사: 통과

