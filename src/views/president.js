import { GOVERNMENT_SEED } from '../data/government-seed.js';
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const list=(title,items,klass='')=>`<section class="content-card president-section ${klass}"><div class="section-title"><h2>${title}</h2><span>${items.length}개</span></div><div class="president-list">${items.map((item,index)=>`<article><span>${String(index+1).padStart(2,'0')}</span><p>${esc(item)}</p></article>`).join('')}</div></section>`;
export function renderPresidentPage(data=GOVERNMENT_SEED){
  const p=data.profile;
  return `<main class="subpage president-page"><section class="president-hero"><div><span class="eyebrow">PRESIDENT OF THE REPUBLIC OF KOREA</span><h1>${esc(p.name)}</h1><strong>${esc(p.office)}</strong><p>${esc(data.vision)}</p><small>${esc(data.sourceLabel)} · ${esc(data.verifiedAt)} 확인</small></div><dl><div><dt>소속</dt><dd>${esc(p.party)}</dd></div><div><dt>출생</dt><dd>${esc(p.birth)}</dd></div><div><dt>학력</dt><dd>${esc(p.education)}</dd></div><div><dt>임기</dt><dd>${esc(p.term)}</dd></div></dl></section><div class="president-page-grid">${list('주요 경력',data.career)}${list('선거 기록',data.elections)}<section class="content-card president-vision"><span class="eyebrow">NATIONAL VISION</span><h2>국정 비전</h2><p>${esc(data.vision)}</p></section>${list('핵심 정책',data.policies)}${list('공약',data.pledges)}${list('국정과제',data.nationalTasks)}</div><section class="content-card president-section"><div class="section-title"><h2>리더십</h2><span>${data.leadership.length}명</span></div><div class="president-people-grid">${data.leadership.map(item=>`<article><span>${esc(item.role)}</span><h3>${esc(item.name)}</h3><p>${esc(item.area)}</p><small>${esc(item.note)}</small></article>`).join('')}</div></section><section class="content-card president-section"><div class="section-title"><h2>정부 구성</h2><span>${data.cabinet.length}개 부처</span></div><div class="president-cabinet-grid">${data.cabinet.map(item=>`<article><span>${esc(item.office)}</span><h3>${esc(item.name)}</h3><b>${esc(item.title)}</b><p>${esc(item.area)}</p></article>`).join('')}</div></section></main>`;
}
