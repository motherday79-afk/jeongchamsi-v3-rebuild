# 정참시 0.0.31.151 — 뒤로가기 화면 복원 수정

이 패치는 0.0.31.150 웹 전체본 위에 적용합니다.

## 확인한 결함과 변경

- 브라우저 뒤로가기로 HTML을 복원하면 `data-free-scroll-ready`, `data-detail47-ready` 표시는 남지만 이벤트 리스너는 복원되지 않았습니다. 기존 코드는 표시만 보고 재연결을 생략했습니다.
- NOW Rank 스와이프 후 발생하는 클릭을 막는 처리, 정치인 상세의 지역 이슈 선택 및 기간 선택 처리를 새로 복원된 DOM에 다시 연결합니다.
- 같은 DOM에는 중복 연결하지 않도록 WeakSet으로 실제 연결된 노드를 구분합니다.
- 색상, 레이아웃, 통합검색 데이터, 비교 연출, 내부 페이지 주소 체계는 변경하지 않습니다.

## 검증

- 수정 전 재현 테스트: 복원된 NOW 스와이프/상세 선택 4개 실패, 수정 후 통과.
- 관련 웹 테스트 75개 통과. 실제 DOM API의 대역을 사용한 코드 테스트이며, 삼성 인터넷 실기 검증을 대신하지 않습니다.
- 압축 파일의 무결성과 150 전체본 + 151 패치 = 151 전체본의 파일별 일치를 검사합니다.

## 아직 확정하지 않은 문제

Gmail·PayPal 연결 프로그램 선택창은 이 패치로 해결됐다고 보장하지 않습니다. 운영 사이트 PC Chrome에서 오세훈 및 잇츠미는 정상 내부 주소로 이동했습니다. 풋터 메일 링크의 터치 가로채기는 확인되지 않아 해당 링크를 임의 삭제하지 않았습니다.

제보 기기에서 먼저 확인할 재현 조건: 새 탭으로 처음 접속한 직후에도 발생하는지, 상세페이지에서 시스템 스와이프로 돌아온 뒤에만 발생하는지.

## 적용

- 현재 150 적용 상태: `JCS_0_0_31_151_PATCH.zip`을 풀어 같은 경로에 덮어쓰고 배포.
- 웹 전체 파일을 교체할 때: `JCS_0_0_31_151.zip`만 사용.
- 별도 Android 소스 패치는 웹 배포로 설치 앱에 반영되지 않습니다. 기존 APK 빌드 저장소에 적용하고 다시 빌드해야 합니다.
