# JCS 0.0.30.3 상세·비교 중복 제거 핫픽스

## 적용 기준

- 기준 패치: `JCS 0.0.30.2`
- 적용 방식: ZIP을 프로젝트 루트에 덮어쓰기
- 배포 후 브라우저 새로고침 권장

## 변경 내용

### 정치인 상세페이지

- 비로그인·일반회원·관리자 화면에서 `핵심 사건`은 01 정치인 브랜드 진단에만 노출
- `변화 원인`은 01 정치인 브랜드 진단에만 노출
- `변화 흐름`, `최근 변화`, 공개 화면의 추세 시각화는 01 정치인 브랜드 진단에만 노출
- 기존 규칙 유지
  - `과거와 현재`, `근거 데이터`: 01번만 노출
  - `서브데이터`: 06번만 노출

### 비교하기

- 비로그인·일반회원·관리자 비교 화면에도 상세페이지와 같은 중복 제거 규칙 적용
- 인물별 `핵심 사건`, `변화 원인`, `과거와 현재`, `최근 변화`, `근거 데이터`는 01번에서만 노출
- `서브데이터`는 06번에서만 노출
- 관리자 비교의 반복 `비교 기준` 제거
- 우위·열세·경합, 점수 차이, 비교 해석, 기회·위험, 전략 처방은 유지
- 분석 계산식, 권한별 데이터 API, 원본 수집 데이터는 변경하지 않음

## 검증 결과

- 전체 자동 테스트: 228개 통과, 실패 0개
- 상세페이지 권한별 렌더링 검사 통과
- 비로그인·회원 2명 비교 렌더링 검사 통과
- 관리자 4명 비교 렌더링 검사 통과
- JavaScript 구문 검사 및 `git diff --check` 통과

