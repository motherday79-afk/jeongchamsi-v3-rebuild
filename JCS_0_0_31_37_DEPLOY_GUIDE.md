# JCS 0.0.31.37 적용 및 인수인계

## 기준본과 작업 위치

- 기준: 사용자가 제공한 JCS_0_0_31_36_MAIN_NAV_NOW_FOOTER_FINAL.zip
- 유지한 작업 폴더: /workspace/scratch/a7c321d80a7c/qa-composite-31-27
- Git 저장소가 아니며 GitHub 재다운로드·체크아웃·초기화는 하지 않았습니다.
- 기존 데이터·정치인 사진·환경변수·Redis 내용은 변경하지 않았습니다.

## 반영한 요청

1. 모바일 계정 영역의 SIGNED IN, 계정명·권한, 마이페이지 버튼을 한 줄로 정리했습니다. 긴 닉네임은 말줄임하며 버튼 공간은 유지합니다.
2. NOW Rank의 구형 1위 강조 배치 및 작은 화면용 가로 카드 규칙을 원본 CSS에서 제거했습니다. 모든 순위에 동일한 사진·이름·정당·지역 배치를 사용합니다.
3. 1024px 이하 또는 터치 전용 화면에서 NOW Rank는 연속 가로 스크롤입니다. 좁은 휴대폰에서는 2장, 펼친 폴드에서는 폭 172px 카드가 연속됩니다. PC는 기존 10명씩 넘기기를 유지합니다.
4. PC 좌우 버튼은 카드 목록과 별도의 42px 그리드 열에 배치되고, 카드와 버튼 사이에 16px 간격을 둡니다. absolute 기준점 충돌을 제거했습니다.
5. PC 메뉴는 76px 타일·8px 간격으로 실제 들어가는 수만 표시합니다. 나머지만 더보기에 표시합니다. 모바일 기존 주요 6개 메뉴와 가로 스크롤은 유지합니다. 모든 서비스 아이콘에 카탈로그 컬러를 적용합니다.
6. '실제 공개 스냅샷으로 비교'를 삭제하고, 비교 설명을 '비로그인, 회원로그인에 따른 차등 정보제공'으로 변경했습니다.
7. 처방 01~10은 좁은 화면에서도 진단처럼 한 줄 10칸을 유지합니다. 기존 5칸씩 두 줄 규칙은 제거했습니다.
8. 브랜드 진단은 PC 3열×2행, 1024px 이하 2열×3행, 560px 이하 1열×6행입니다. 여섯 칸의 기본 높이 310px와 내부 정렬을 맞추며, 글자 확대에 따라 모든 칸이 함께 늘어날 수 있습니다.
9. 선명도 4개 원형 지표, 정책 연결도 분절 막대, 이미지 일관성 10개 구간, 본인·평균 비교, 확장성 레이더, PC·MOBILE 검색량은 기존 데이터에 연결됩니다. 시안 예시 수치는 운영 코드에 삽입하지 않았습니다.
10. 검색량은 항상 K 단위입니다. 12000→12K, 36500→36.5K, 1200000→1200K, 320→0.32K. 툴팁과 접근성 설명에는 정확한 원래 수치를 유지합니다.
11. 세대별 대통령 관리자 후보 등록 입력칸을 기존 4명에서 서버 지원 한도인 15명까지 확대했습니다. 새 모의투표를 작성하고 메인에 적용할 수 있으며, 기존 모든 세대 투표 화면에 연결됩니다.

## 미반영 범위

- 관리자 임의 득표를 실제 시민 참여 집계처럼 공개하는 기능은 추가하지 않았습니다.
- 투표·공감·전국평가 집계 데이터와 집계 로직은 그대로 유지했습니다.
- 실제 폴드/PC 브라우저 스크린샷 검증은 완료하지 못했습니다. 검증 브라우저가 로컬 주소 접속을 차단했습니다.
- Redis 실사용량·키 중복·TTL 실검사는 이번 화면 패치 범위가 아닙니다.

## 검증

- npm test: 전체 461개 통과, 실패 0개.
- 검색량 단위 경계, 폴드 펼침↔PC 전환, PC 페이지 이동, 메뉴 수용량, 관리자 후보 15칸/일반 사용자 비노출 검증.
- 기존 진단 데이터·정치인 사진·게시물·인증·집계 회귀 테스트 포함.
- 검증 자료 ZIP에는 테스트 소스와 실행 로그를 따로 제공합니다. 배포용 ZIP은 기존과 동일한 99개 파일 구성입니다.

## 배포

1. 배포용 JCS_0_0_31_37.zip을 풀어 같은 경로에 반영합니다. 검증 자료 ZIP은 배포용이 아닙니다.
2. DELETE_OBSOLETE_FILES_31_37.txt에 적힌 구형 CSS가 운영 저장소에 남아 있으면 제거합니다.
3. 환경변수와 Redis 데이터는 유지합니다.
4. 커밋/배포 후 표시 버전 JCS_0_0_31_37을 확인합니다.
5. 폴드 접힘·펼침 전환과 PC 화살표의 카드 비중첩, 메뉴 더보기, 브랜드 6칸을 실제 기기에서도 확인합니다.

## 다음 작업 원칙

현재 폴더를 유지하고, 이전 ZIP·구형 CSS로 덮어쓰지 않습니다. 승인 시안의 크기·배치·그래프 표현을 임의 변경하지 않습니다. 31.37은 새 hotfix CSS를 추가하는 대신 기존 파일에서 충돌 규칙을 제거·교체했으므로, 수정된 CSS 전부를 함께 반영해야 합니다.
