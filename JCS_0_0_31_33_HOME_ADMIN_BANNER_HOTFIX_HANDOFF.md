# JCS 0.0.31.33 적용 안내

## 반영 내용

- 메인 NOW Rank 카드의 `NOW 99.0` 형태 점수 표시를 모두 제거했습니다. 점수는 순위 정렬에만 사용합니다.
- IT’S ME, 시티즌 초이스, 전국 평가제, 세대별 대통령, 커뮤니티의 여백과 중복 안내를 줄여 메인 세로 길이를 압축했습니다.
- 세대별 대통령의 큰 소개 블록을 제거하고 참여 버튼을 헤더로 이동했습니다. 데스크톱은 6개 결과를 한 줄, 모바일은 가로 스크롤로 표시합니다.
- 관리자 YouTube 자동 등록 콘솔과 자동 재개 동작을 제거했습니다. 기존 등록 채널은 유지되며 정치인 정보 관리에서 URL, `@handle`, UC 채널 ID를 수동 저장하거나 삭제할 수 있습니다.
- 전체 및 한 명 수집본은 자동 검증을 통과하면 별도 `검수 승인` 없이 게시할 수 있습니다. 검토·수정 화면과 게시 전 자동 검증 차단은 유지합니다.
- `COHORT_VECTOR_CLONED`는 연령별 남녀 값 쌍이 전부 완전히 같을 때만 복제로 차단하도록 수정했습니다. 정상적인 저변동 데이터는 더 이상 실패하지 않습니다.
- `assembly-026`과 `assembly-211`은 서로 다른 동명이인 박지원 의원이므로 두 프로필 모두 유지합니다.
- 메인 배너 업로드에 Vercel Blob 토큰 전달과 OIDC 연결 판별을 추가했습니다. 관리자 등록창에서 저장소 연결 상태를 먼저 표시합니다.

## 배너 규격

- 권장 크기: 640 × 350px
- 허용 형식: JPG, PNG, WEBP
- 최대 파일 크기: 2MB
- 클릭 URL: `https://` 주소만 허용
- 저장소 미연결 시 `배너 저장소 미연결 · Vercel Blob 연결 및 재배포 필요`로 안내

## 적용 순서

1. ZIP을 압축 해제하고 현재 0.0.31.32 프로젝트 최상단에 폴더 구조 그대로 덮어씁니다.
2. 업로드/커밋 후 자동 배포가 시작되는지 확인합니다.
3. 자동 배포가 시작되지 않은 경우에만 Actions의 해당 배포 워크플로에서 `Run workflow`를 누릅니다.
4. 배포 완료 후 브라우저를 강력 새로고침하고 관리자 화면 버전 `JCS_0_0_31_33`을 확인합니다.
5. 관리자 메인 배너 등록창에서 `배너 저장소 연결됨 · 최대 2MB`가 표시되는지 확인합니다.

## 운영 확인

- 기존 34개 YouTube 채널 연결은 삭제하지 않습니다.
- 한 명 수집 후 자동 검증 통과 시 바로 `게시`를 누릅니다.
- 전체 수집 완료 후 자동 검증이 통과되면 `전체 게시`가 활성화됩니다.
- 실제 손상·누락 데이터는 자동 검증에서 계속 게시 차단됩니다.

