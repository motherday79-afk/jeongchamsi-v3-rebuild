# JCS 0.0.31.29 사진·글자 크기·대표배지 핫픽스

이 패키지는 GitHub에서 다시 받은 소스가 아니라, 기존 `31.28` 작업 결과 위에 그대로 이어서 만든 누적 덮어쓰기 패키지입니다.

## 적용 내용

- 관리자 정치인 사진 등록 오류 개선
  - Vercel Blob 연결 정보가 없을 때 원본 영문 오류를 그대로 노출하지 않습니다.
  - 관리자 화면에 `사진 저장소가 연결되지 않았습니다. Vercel Blob을 연결한 뒤 다시 배포해 주세요.`라고 명확히 안내합니다.
  - JPG·PNG·WEBP, 최대 1MB, 이미지 시그니처 검증과 기존 사진 교체 흐름은 유지합니다.
- 상단 글자 크기 조절 UI 정리
  - 기존 외곽 박스, 버튼 박스, `기본` 표기를 모두 제거했습니다.
  - `글자 크기  −  ＋`만 남기고 기존 5단계 확대·축소 동작을 유지했습니다.
  - 같은 위치의 `후원하기` 문구와 링크를 제거했습니다.
- 회원 대표배지 작성자 표기
  - 게시글 목록, 게시글 상세, 댓글, 메인 칼럼·커뮤니티 미리보기에서 닉네임 바로 옆에 대표배지가 표시됩니다.
  - 글 작성 당시 배지를 복사해 저장하지 않고, 화면을 불러올 때 회원의 현재 대표배지를 붙입니다.
  - 따라서 회원이 대표배지를 바꾸면 기존 작성글에도 새 대표배지가 일관되게 반영됩니다.
- 릴리스 및 캐시 버전 `JCS_0_0_31_29` 적용

## 이번 패키지에서 제외한 내용

- 영어·일본어·중국어 자동 번역은 요청대로 보류했습니다.
- 메인페이지 타이포그래피 정돈은 승인 전 시각 제안만 만들었으며 실제 화면에는 아직 적용하지 않았습니다.

## 사진 업로드 배포 필수 조건

코드만 덮어써도 Vercel Blob 저장소가 연결되지 않은 배포 환경에서는 사진을 저장할 수 없습니다. Vercel 프로젝트에서 Blob Store를 연결한 뒤 같은 프로젝트를 다시 배포해야 합니다. 연결이 끝나면 업로드 코드는 발급된 Blob 자격 증명을 자동으로 사용합니다.

## 적용 방법

ZIP의 폴더 구조를 유지한 채 현재 프로젝트 루트에 덮어씁니다. 저장소를 새로 체크아웃하거나 초기화하지 않습니다.

## 검증

- 사진 오류 처리, 글자 크기 UI, 대표배지 API·목록·상세·댓글·메인 미리보기 관련 테스트: 55개 통과, 실패 0개
- 변경 JavaScript 7개 파일 구문 검사 통과
- 전체 합성 작업본: 401개 중 390개 통과
- 나머지 11개는 이번 수정 전부터 존재한 진단·매체 시안 고정 회귀 테스트이며 이번 세 기능의 관련 테스트에는 포함되지 않습니다.

