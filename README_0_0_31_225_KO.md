# JCS 0.0.31.225 · 폴리마블 보드 제작 STEP 1

이번 패치는 **게임 구현 패치가 아닙니다.**
요청한 순서대로 보드 베이스와 32개 독립 객체만 정참시에 등록하는 단계입니다.

## 이번 단계에서 한 것
- 기존 31.224 폴리마블 화면/HUD/게임 UI를 `/polimable`에서 렌더링하지 않음
- 새 승인 이미지 `polimable-board-base-31-225.png`를 보드의 유일한 베이스 이미지로 사용
- 보드 칸을 정확히 **32개 독립 DOM 객체**로 분리
- 큰 모서리 객체: **0 / 8 / 16 / 24**, 총 4개
- 각 객체에는 현재 **명칭 / 아이콘 / 이미지 / 가격 / 기능 / 클릭 / 게임룰이 전혀 없음**
- 게임 인터랙션 hydrate/bind는 31.225에서 의도적으로 NO-OP 처리
- PC/모바일에서 동일 보드가 비율을 유지하며 축소/확대됨

## 매우 중요: 기존 이미지 삭제
GitHub에 반영할 때 아래 파일은 같은 커밋에서 삭제해야 합니다.

`assets/polimable/DESIGN_SOURCE_OF_TRUTH_31_224.png`

새 페이지는 아래 파일만 사용합니다.

`assets/polimable/polimable-board-base-31-225.png`

즉, 이전 이미지를 뒤에 남겨두고 새 이미지를 덮는 방식이 아닙니다.

## 파일 적용
ZIP을 프로젝트 루트 기준으로 덮어쓴 뒤, `DELETE_BEFORE_COMMIT_31_225.txt`의 기존 파일을 삭제하고 커밋/배포하세요.

## 이 단계에서 하지 않은 것
- 칸 이름
- 칸 아이콘
- START/광장/운명/투어 명칭
- 캐릭터
- HUD
- 주사위
- 민심
- 구매/강화/인수
- 카드/특수효과
- 카메라 이동

다음 단계에서 승인된 순서대로 하나씩 올리기 위한 **빈 보드 객체 구조**까지만 포함합니다.
