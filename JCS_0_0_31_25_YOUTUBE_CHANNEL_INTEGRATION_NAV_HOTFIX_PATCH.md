# JCS 0.0.31.25 — 공식 유튜브 채널 연동 · 처방 바로가기 · 글자 크기 위치

## 이번 적용 범위

- GitHub 재체크아웃·초기화·pull 없이 기존 누적 작업 디렉터리와 31.24 결과물에서 이어서 작업했다.
- 정치인 공식 유튜브 채널을 자동 검색해 즉시 등록하고, 관리자가 잘못된 연결을 직접 수정·재검색·삭제할 수 있게 했다.
- 수집 범위는 공식 채널, 공개 영상, 영상 조회수, 24시간·7일·30일 업로드 수와 최근 30일 업로드 흐름으로 제한했다.
- 구독자, 좋아요, 댓글, 감성 분석, 영상 업로드, YouTube 계정 로그인 기능은 넣지 않았다.
- 07 언론·온라인 영향력 진단에는 실제 공식 채널 데이터가 수집된 정치인에게만 유튜브 영역을 표시한다. 데이터가 없으면 빈 박스나 연결 대기 문구도 만들지 않는다.
- 비로그인·일반회원·관리자별 기존 진단 공개 범위는 그대로 유지하고 최근 영상은 각각 3개·5개·10개로 차등 제공한다.

## 관리자 자동 등록과 교정

1. 관리자 화면의 `YOUTUBE CHANNEL DATA`에서 미등록 정치인의 공식 채널 자동 등록을 시작한다.
2. 이름·정당·직책·지역을 함께 검색하고 가장 강한 일치 후보를 자동 저장한다.
3. 한 API 요청에서 최대 5명만 처리하고 처리 위치를 저장하므로 중단 후 이어서 실행할 수 있다.
4. 현재 YouTube 기본 검색 한도에 맞춰 하루 최대 100명까지만 자동 검색하고 한도에 도달하면 `PAUSED_QUOTA`로 멈춘다.
5. 잘못 연결된 채널은 채널 URL, `@handle`, 채널 ID 중 하나를 입력해 관리자 지정값으로 저장한다.
6. `다시 검색`은 해당 정치인만 자동 검색하고, `삭제`는 저장된 연결을 지워 다음 자동 등록 대상에 포함한다.

## 상세페이지 반영 순서

채널 등록만으로 기존 공개 스냅샷이 자동 변경되지는 않는다. 배포 후 아래 순서로 새 데이터를 게시한다.

1. 유튜브 API 키 연결 및 재배포
2. 관리자 화면에서 공식 채널 자동 등록 및 오등록 교정
3. `전체 수집` 실행
4. 수집 완료 후 대표 초안 검수 및 `검수 승인`
5. `전체 게시` 실행
6. 공개 상세페이지에서 07번 유튜브 채널·영상·조회수·업로드 흐름 확인

전체 542명을 기본 검색 한도만으로 자동 등록하려면 여러 할당량 날짜에 걸쳐 `이어서 등록`해야 한다. 먼저 등록된 정치인은 그날 바로 수집·검수·게시할 수 있다.

## 사용자가 해야 할 YouTube API 등록

1. Google Cloud Console에서 프로젝트를 새로 만들거나 기존 프로젝트를 선택한다.
2. `API 및 서비스 > 라이브러리`에서 `YouTube Data API v3`를 검색해 사용 설정한다.
3. `API 및 서비스 > 사용자 인증 정보 > 사용자 인증 정보 만들기 > API 키`를 선택한다.
4. 생성된 키의 `API 제한사항`을 `키 제한`으로 바꾸고 `YouTube Data API v3`만 선택해 저장한다.
5. 정참시 Vercel 프로젝트의 `Settings > Environment Variables`에 이름 `YOUTUBE_DATA_API_KEY`, 값에는 발급받은 API 키를 등록한다. Production은 반드시 선택하고 Preview에서도 검사하려면 Preview도 선택한다.
6. 키를 소스 코드, GitHub, 브라우저 설정에 붙여 넣지 않는다. 이 구현은 서버에서만 키를 읽는다.
7. Vercel에서 새 배포를 실행한 뒤 관리자 화면에 `API 연결됨`이 표시되는지 확인한다.

이 연동은 공개 데이터 읽기만 사용하므로 OAuth 동의 화면이나 YouTube 계정 로그인이 필요하지 않다. Vercel의 고정 아웃바운드 IP를 별도로 구성한 경우에만 Google Cloud의 IP 애플리케이션 제한도 추가한다. 고정 IP가 없다면 API 제한을 반드시 적용하고 Google Cloud의 할당량 화면을 함께 확인한다.

## UI 핫픽스

- 정치전략처방 01~10 바로가기는 진단과 동일한 전체 폭 10칸으로 맞췄다.
- 모바일에서는 좌우 여백 없이 5×2 그리드로 내려가며 5번·10번 우측 경계와 두 번째 줄 상단 경계를 바로잡았다.
- 처방 전체보기는 기존 펼침 폭을 유지하고 같은 위치에서 아래로 펼쳐진다.
- 글자 `− / ＋` 조절기는 별도 페이지 상단 줄에서 제거하고 `명이 정참시와 함께합니다` 우측의 기존 `정참시 응원하기` 위치로 옮겼다.
- 글자 크기 기본+5단계, 저장, 상·하한, 페이지 이동 후 복원 동작은 유지했다.

## 검증 결과

- 전체 자동 테스트 410개 통과
- YouTube API 키 비노출, 안정 오류 코드, 자동 등록 5명 단위, 수동값 보호, 재검색, 삭제, 일 100회 정지·다음 할당량 날짜 재개 검증
- YouTube 수집 실패가 네이버·Google 뉴스 수집 결과를 폐기하지 않는 독립 실패 격리 검증
- 비로그인 3개·일반회원 5개·관리자 10개 영상 공개 범위 검증
- 실제 YouTube 데이터가 없을 때 빈 상세 모듈 비생성 검증
- JavaScript 구문, CSS 괄호, Git 공백, ZIP CRC와 원본 바이트 검증

