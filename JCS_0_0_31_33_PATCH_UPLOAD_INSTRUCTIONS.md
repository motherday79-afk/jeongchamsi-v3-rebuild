# JCS 0.0.31.33 소형 패치 업로드

이 ZIP은 직전 정참시 0.0.31.32 위에 덮어쓰는 패치입니다.

1. ZIP을 PC에서 압축 해제합니다.
2. 압축 안의 `api`, `css`, `lib`, `src`, `tests` 폴더와 루트 파일을 저장소 최상단에 같은 경로로 올립니다.
3. 같은 이름의 파일은 모두 덮어씁니다.
4. 커밋 후 배포 작업이 자동으로 시작되는지 확인합니다.
5. 자동 실행되지 않을 때만 해당 GitHub Actions 워크플로에서 `Run workflow`를 누릅니다.
6. Vercel 배포 완료 후 관리자 화면 버전이 `JCS_0_0_31_33`인지 확인합니다.

배너 업로드에는 Production 환경의 Vercel Blob 연결이 필요합니다. 등록창에서 `배너 저장소 연결됨 · 최대 2MB`가 보이면 준비된 상태입니다.

