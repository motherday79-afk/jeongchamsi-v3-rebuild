# JCS 0.0.31.13 — COHORT SCORE RANGE HOTFIX

## 원인

연령·성별 지지구조 계산에서 정당 기준선에 개인 NOW 신호, 뉴스 프레임, 연령·성별 직접 언급값을 더한 뒤 최종 범위를 제한하지 않아 일부 결과가 100을 초과했습니다. 검증기는 정상적으로 이를 `COHORT_SCORE_INVALID`로 차단했고 해당 정치인의 초안 생성이 실패했습니다.

## 수정

- 모든 연령·성별 최종 점수를 `0~100` 범위로 확정한 뒤 검증 및 저장
- 정당 기준선과 정치인별 검색·뉴스 보정 구조는 그대로 유지
- 542명 전체를 최대 수집 신호 조건으로 생성해 범위 이탈 여부 검증
- 기존 성공 정치인은 보존하고 실패 정치인만 다시 처리하는 관리자 재시도 흐름 유지
- 관리자 표시 버전을 `JCS_0_0_31_13`으로 갱신

## 적용 순서

1. ZIP의 파일을 저장소 루트에 덮어씁니다.
2. GitHub에 등록하고 배포합니다.
3. 관리자 페이지에서 강력 새로고침 후 `JCS_0_0_31_13` 표시를 확인합니다.
4. 어제 실패 기록이 남아 있으면 `실패 항목만 재시도`를 누릅니다.
5. 재시도가 끝나면 검토·승인 후 게시합니다.

기존 수집 작업이 삭제됐거나 실패 재시도 버튼이 보이지 않을 때만 전체 수집을 다시 실행합니다.

## 검증 결과

- 전체 자동 테스트: 332 통과 / 0 실패
- 542명 일반 데이터 생성·검증 통과
- 542명 최대 뉴스·검색 신호 경계값 생성·검증 통과
- JavaScript 구문 검사 통과
- 변경 파일 공백 오류 검사 통과

