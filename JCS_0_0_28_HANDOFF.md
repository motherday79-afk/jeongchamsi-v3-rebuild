# JCS 0.0.28 권한별 정치 인텔리전스 패치 인수인계

## 적용 기준

- 기준 버전: `JCS 0.0.27.2`
- 적용 버전: `JCS 0.0.28`
- 배포 방식: 저장소 루트에 패치 ZIP의 디렉터리 구조를 유지해 덮어쓰기
- GitHub 커밋·푸시는 수행하지 않음

## 구현 결과

- 정치 진단 10개 항목과 권한별 공개 범위를 `lib/intelligence-access.js` 한 곳에서 관리한다.
- 비로그인 상세·비교: `01, 07, 09`와 공개 필드만 서버 응답에 포함한다.
- 일반회원 상세·비교: `01, 02, 03, 05, 07, 09`와 해석 필드만 서버 응답에 포함한다.
- 관리자 상세·비교: `01~10`과 진단·전략·실행 처방 필드를 서버에서 권한 확인 후 응답한다.
- 상세페이지와 비교하기가 같은 서버 투영 결과와 같은 항목 값을 사용한다.
- 비로그인·일반회원 비교는 최대 2명, 관리자는 최소 2명부터 최대 4명으로 제한한다.
- 비교 결과는 항목별 매트릭스로 구성하고 정치인 사진·소속·직책·현재 위치·추세 헤더를 유지한다.
- 관리자 화면은 공개 상세페이지보다 불필요하게 커지지 않도록 보고서형 고밀도 스타일을 적용했다.
- 관리자 원자료를 브라우저에 보낸 뒤 CSS로 숨기지 않는다. 응답 생성 단계에서 허용 필드만 선택한다.
- 중첩 지표, 뉴스, 출처, 연관 정치인에도 허용 필드 목록을 적용해 임의의 비공개 속성이 섞이지 않게 했다.
- 빈 문자열이나 미확보 값을 `0`으로 변환하지 않으며, 데이터가 없으면 `분석 준비 중`, `비교 가능한 데이터 부족`, `해당 기간 데이터 없음`으로 표시한다.
- 기존 프로필·경력·정치 기록·사진·탭·검색·자동완성·최근 본 정치인·비교 대상 추가/삭제 흐름은 유지한다.

## 권한 검수 결과

| 구분 | 상세페이지 | 비교하기 | 서버 응답 |
|---|---|---|---|
| 비로그인 | 3개 항목 | 최대 2명·3개 항목 | 공개 필드만 포함 |
| 일반회원 | 6개 항목 | 최대 2명·6개 항목 | 관리자 전략·처방 제외 |
| 관리자 | 10개 항목 | 2~4명·10개 항목 | 관리자 권한에서만 전체 필드 포함 |

## 테스트 결과

- 전체 자동화 회귀 테스트: `208 passed / 0 failed`
- 권한·API·상세·비교 집중 테스트: `25 passed / 0 failed`
- JavaScript 구문 검사: 통과
- Git 패치 공백 오류 검사: 통과
- 5명 비교 잔여 운영 코드 검사: 통과
- 데스크톱·모바일 반응형 계약: 4인 매트릭스, 고정 헤더, 안전한 가로 스크롤 및 모바일 열 너비 테스트 통과

## 현재 데이터로 확정할 수 없는 항목

- 실제 30일·90일·1년 시계열이 없는 정치인은 해당 기간 데이터를 표시할 수 없다.
- 동급 정치인 전체 모집단 데이터가 없는 항목은 백분위와 비교 순위를 산출하지 않는다.
- 지역구별 민심, 정책 발표 전후 계층 반응, 예상 변화 수치가 원자료에 없으면 임의 생성하지 않는다.
- 실행 후 예상 효과와 추적 지표도 근거 데이터가 없는 경우 준비 상태로 표시한다.

## 배포 후 실제 화면 확인 필요

작업 환경의 격리된 브라우저에서는 아직 배포되지 않은 로컬 사이트와 실제 로그인 세션에 접근할 수 없었다. 따라서 비로그인·일반회원·관리자 각각의 실제 배포 URL 화면 검수는 GitHub 반영 후 남은 단계다. 자동화 테스트에서는 세 권한의 서버 응답, 렌더링 결과, 항목 수, 비교 인원 제한, 사진 유지, 모바일 매트릭스 안전 규칙을 검증했다.

## 적용 파일

- `api/gateway.js`
- `css/pages.css`
- `index.html`
- `lib/intelligence-access.js`
- `package.json`
- `package-lock.json`
- `src/app.js`
- `src/data/kim-minseok-pilot.js`
- `src/views/politician-compare.js`
- `src/views/politicians.js`
- `tests/0-0-27-1-corrections.test.js`
- `tests/compare.test.js`
- `tests/intelligence-access.test.js`
- `tests/intelligence-published-view.test.js`
- `tests/politicians.test.js`
- `tests/politician-diagnostics-api.test.js`
- `tests/politician-diagnostics-compare.test.js`
- `tests/politician-diagnostics-css.test.js`
- `tests/politician-diagnostics-view.test.js`
