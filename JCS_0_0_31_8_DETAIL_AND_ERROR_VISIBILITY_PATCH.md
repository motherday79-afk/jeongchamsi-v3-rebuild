# JCS 0.0.31.8 Detail & Error Visibility Patch

## 적용 내용

- Google RSS 게시일을 ISO 시각으로 정규화해 DIAGNOSIS 01·06의 24H/7D/30D 집계가 0으로 고정되는 문제 수정
- DIAGNOSIS 02를 `세대 합계 100%`와 `세대 내부 남성·여성 합계 100%`의 2단계 구조로 변경
- DIAGNOSIS 03 지역 의제에 실제 기사 제목·링크를 연결하고 지역 보도/본인 메시지 겹침 막대와 격차(%p)로 교체
- DIAGNOSIS 04 명칭을 코어·유동·이탈로 통일하고 합계 문구 삭제
- DIAGNOSIS 05 뉴스 프레임 숫자 가독성 확대
- DIAGNOSIS 06 지속력을 최초·최근·지속일·재점화 횟수와 누적 확산 곡선으로 교체
- DIAGNOSIS 07 검색량을 K/M 축약 표기하고 정확한 원수치는 접근성 라벨과 툴팁에 유지
- DIAGNOSIS 08 공식 선거 레코드의 득표율·경쟁자 득표율로 격차를 계산하고 지역별 우세·경합·취약을 표시
- DIAGNOSIS 09 완료 단계까지의 레일에서 완료·현재·향후 단계를 명확히 구분
- 관리자 회원 목록에서 일반회원/관리자 권한 변경 지원
- 관리자 자기 자신의 관리자 해제와 마지막 관리자 해제를 서버에서 차단
- 수집 오류에 정치인명·ID·단계·코드·상세·시간·시도 횟수·재시도 여부 표시
- 실패 항목만 재시도하며 기존 성공 결과를 보존
- 오류 상세는 항목당 최대 240자로 제한하고 과거 스냅샷을 추가 보관하지 않음

## 배포 후 순서

1. GitHub에 ZIP 내용을 덮어씁니다.
2. 배포 완료 후 관리자 페이지를 강력 새로고침합니다.
3. 기존 완료 스냅샷을 다시 게시하지 말고 `전체 수집`으로 새 스냅샷을 생성합니다.
4. 오류가 남으면 `실패 항목만 재시도`를 누릅니다.
5. 검수 통과 후 승인하고 `전체 게시`를 누릅니다.

## 실제 데이터 제한

- 선거 득표율·경쟁자·지역별 개표값은 정치인 프로필 또는 공식 선거 입력 레코드에 실제 값이 연결된 경우에만 표시합니다.
- 값이 없는 정치인에게 임시 숫자나 추정 득표율을 만들지 않습니다.

## 검증

- `npm test`: 298개 통과, 0개 실패
- 변경 JavaScript: `node --check` 통과
- ZIP: 이미지 파일 미포함
