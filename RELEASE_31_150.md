# 정참시 0.0.31.150

## 이번 변경

### 언론 분석 보완

‘OO언론의 시선’ 안에 ‘보도 비중의 차이’를 추가했습니다. 검색한 정치인 또는 정당을 선택한 언론사가 다룬 기사 비중과, 해당 언론사를 제외한 다른 언론의 합산 비중을 나란히 보여줍니다.

- 실제 기사 건수 / 전체 비교 기사 건수, 비중, 비중 차이(%p)를 함께 표시합니다.
- 양쪽 전체 기사 각각 20건 이상이며 대상 기사 각각 5건 이상일 때만 배수를 표시합니다. 0건인 비교 대상에 무한대 배수를 만들지 않습니다.
- 비중 차이가 큰 다른 인물도 확인할 수 있고, 인물을 누르면 같은 언론사·기간으로 이어집니다.
- 최신순은 최근 7일, 누적순은 최근 30일입니다. 월간 관심도는 기존 기준을 유지합니다.
- 원언론사 미확인 기사는 양쪽에서 제외합니다. 동일 기사는 중복 집계하지 않으며 정당 소속 여러 인물이 함께 나온 기사도 해당 정당 기사로는 한 번만 집계합니다.
- 전체 언론 보도 전수가 아닌 정참시 등록 정치인의 수집 기사 범위입니다. 계산 기준·표본·관련 기사 근거를 펼쳐 확인할 수 있습니다. 지지나 반대를 추정하는 기능은 아닙니다.

### 메인 정치인 비교분석 연출

기존 옆에서 나타나는 효과를 제거하고, 위에서 낙하해 지면에 안착하는 연출로 교체했습니다.

- 각 정치인 선택 시 낙하, 착지 반동, 지면 균열, 파편과 충격파가 이어집니다.
- 좌우 정치인이 모두 착지를 끝내면 퍼플·골드 에너지가 중앙에서 만나고 VS가 아래에서 튀어 올라옵니다.
- 첫 화면은 빈 선택 상태이며, 한 명만 선택했을 때는 VS가 나오지 않습니다.
- 오른쪽부터 선택, 빠른 연속 선택, 재검색·선택 변경, 화면 이탈 시 오래된 연출이 뒤늦게 실행되지 않도록 정리합니다.
- 비교 박스 높이는 252px을 유지하고, 검색창은 별도 떠 있는 패널로 엽니다. + 버튼과 선택 버튼, 검색, 비교 실행은 연출 중에도 사용할 수 있습니다.
- 기기의 동작 줄이기 설정을 사용하면 낙하·충돌 대신 안정된 최종 선택 상태를 표시합니다.
- 영상·외부 이미지 다운로드 없이 SVG와 브라우저 애니메이션으로 구성했습니다.

## 적용 방법

- 31.149 적용 상태: `JCS_0_0_31_150_PATCH.zip`을 풀어 같은 경로에 덮어씁니다.
- 전체 파일 교체: `JCS_0_0_31_150.zip`을 사용합니다. FULL과 PATCH를 동시에 적용할 필요는 없습니다.
- 새로 포함된 JavaScript 파일도 함께 올려야 합니다. 변경된 CSS·JavaScript의 캐시 버전을 갱신했습니다.
- 실제 GitHub 업로드와 Vercel 배포는 이 작업에서 실행하지 않았습니다.

## 검증 범위와 한계

계산·렌더링·선택 순서·취소·동작 줄이기·기존 데이터 처리의 자동 검증, JavaScript 문법 및 배포 파일의 참조 경로 검증을 수행했습니다. PATCH를 이전 버전에 적용한 결과와 FULL 파일이 같은지, 압축파일이 정상인지 확인했습니다.

이 환경에는 실행 가능한 브라우저가 없어 실제 PC·모바일 화면, 터치 조작, 기기별 애니메이션 프레임률은 검증하지 못했습니다. 반응형 CSS와 동작 로직 검토를 실기기 확인으로 표현하지 않았습니다.

기존 레이아웃 관련 검사에는 이번 수정 전 31.149 상태에서도 실패하는 항목 12건이 있었습니다. 현재 홈에서 제거된 세대별 대통령, 이전 헤더·네비게이션 구조 등 과거 기대값과 검색 URL 순서 검사이며, 이를 이번 패치에서 임의로 통과 처리하지 않았습니다.

최종 관련 검증: **144건 통과, 0건 실패**. 별도로 확인한 기존 레이아웃 검사 97건은 85건 통과·12건 기존 실패로, 수정 전과 실패 항목이 동일했습니다. PATCH 20개 파일, FULL 156개 파일이며 문법·참조 경로·압축 무결성·이전 버전 위 PATCH 적용 결과와 FULL의 바이트 일치 검사를 통과했습니다.
