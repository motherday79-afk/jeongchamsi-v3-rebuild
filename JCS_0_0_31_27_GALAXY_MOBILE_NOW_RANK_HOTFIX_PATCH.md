# JCS 0.0.31.27 — Galaxy 모바일 NOW RANK 핫픽스

## 기준과 보존 원칙

- 사용자가 첨부한 `JCS_0_0_31_26_ADMIN_OPERATIONS_NEWS_YOUTUBE_HOTFIX_FINAL.zip`을 그대로 기준으로 작업했다.
- GitHub clone, pull, reset, checkout, init은 수행하지 않았다.
- 31.26의 관리자 운영, 뉴스 기간 집계, YouTube, 진단·처방, 권한별 노출 코드는 유지했다.
- 이번 변경은 메인 `NOW RANK` 모바일 레이아웃과 릴리스 표시에만 한정했다.

## 확인된 원인

- 560px 이하의 고우선순위 규칙이 NOW 카드의 모바일 배치를 다시 덮어쓰고 있었다.
- 340px 이하에서는 바깥 랭킹 그리드는 2열로 남지만 카드 내부만 가로형 2열로 바뀌어, 텍스트 영역이 한 글자 폭까지 줄어들었다.
- `overflow-wrap:anywhere`가 적용된 소속·지역 정보가 좁은 칸에서 세로로 붕괴했고 카드 높이도 비정상적으로 늘어났다.

## 수정 내용

- 341~560px: 승인된 10명·2열 NOW 카드 구성을 유지한다.
- 이름, 정당·지역, NOW 점수를 가로쓰기 한 줄로 고정하고 긴 값만 말줄임 처리한다.
- 모든 카드의 사진, 순위, 정당 배지, 텍스트 정렬을 같은 구조로 통일한다.
- 340px 이하: 삼성 화면 확대와 Fold 커버 폭을 위해 1열 가로형 카드로 전환한다.
- 페이지 글자 크기 5단계 확대 기능은 유지하며 `text-size-adjust:none`은 사용하지 않는다.
- 새 스타일 파일을 모든 기존 스타일 다음에 로드해 이전 모바일 선택자가 다시 덮지 못하게 했다.

## 배포

1. ZIP의 내부 파일을 현재 정참시 저장소 최상단에 그대로 덮어쓴다.
2. GitHub에서 변경 파일을 커밋한다.
3. Vercel 배포 완료 후 브라우저를 강력 새로고침한다.
4. 갤럭시에서 메인 NOW RANK 1~10위와 좌우 페이지 이동을 확인한다.

## 검증

- 모바일 NOW 전용 회귀검사 4개 통과
- 레이아웃·관리자 버전·NOW 관련 영향권 테스트 40개 통과
- 360px, 412px, 480px는 2열 계약 검증
- 320px 및 340px 이하는 1열 계약 검증
- 글자 가로쓰기, 줄바꿈 방지, 말줄임, 페이지 가로 넘침 방지 규칙 검증
- JavaScript 구문, CSS 중괄호, ZIP CRC 및 패키지 바이트 무결성 검증

