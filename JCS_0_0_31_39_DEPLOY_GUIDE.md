# JCS 0.0.31.39 적용 안내

기준본: JCS_0_0_31_38.zip. 기존 작업 폴더에서 수정했으며 배포 파일은 동일한 99개 경로 구성입니다. 이전 버전의 화면·권한·사진 업로드 개선을 포함합니다.

## 반영 내용

1. 오른쪽/모바일 계정 카드에서 `내 참여 · 배지`, `MY` 제목과 해당 줄을 삭제했습니다. 대표·전시 배지와 마이페이지 이동은 유지합니다.
2. `급상승 정치인` 메뉴에서 이전 게시 순위 대비 상승폭 순으로 최대 100명을 표시합니다. NOW Rank와 같은 카드 렌더러를 사용하며 최초 30명, 이후 30·30·10명씩 더보기로 호출합니다. 중간에 게시 순위가 바뀌면 목록을 섞지 않고 새로고침 안내를 표시합니다. 상승한 인물이 100명 미만이면 실제 인원만 표시합니다.
3. 정치키워드는 수집 기사 제목을 바탕으로 최근 7일/이전 7일 기사 수, 최근 매체 수와 증가분으로 정렬합니다. 메인은 10개, 전체 화면은 최대 50개이며 관련 정치인·뉴스를 연결했습니다. `관리자 → 정치키워드`에서 숨김·표현 수정·유사어 통합·상단 고정이 가능합니다. 같은 기사에 통합어가 여러 번 나와도 한 번만 집계합니다.
4. `관리자 → 풋터정보` 탭이 정상 경로로 이동하도록 수정했습니다. 사이트명, 대표자, 사업자명, 사업자등록번호, 통신판매업번호, 전화·팩스·주소, 개인정보 책임자·이메일, 광고·고객센터·제휴·경영지원 이메일, 하단 안내 문구를 입력할 수 있습니다. 참고 이미지의 타사 정보는 복사하지 않았습니다. 입력된 항목만 공개 풋터에 묶어 표시합니다.
5. 관리자 탭은 선택한 데이터 하나만 요청합니다. 회원/정치인/풋터 등 조회 결과는 30초, 수집 상태는 5초 동안 재사용하며 저장 후에는 캐시를 비웁니다. 화면 틀은 먼저 표시합니다. 무거운 전체 진단 검토는 `분석 미리보기 불러오기` 버튼을 눌렀을 때 조회합니다. 수집 탭을 떠나면 진행 중인 한 번의 요청까지 마치고 추가 호출을 멈추며, 돌아오면 저장된 위치에서 재개합니다.
6. 회원가입과 마이페이지 주소 수정에 시 아래 구 선택을 추가했습니다. 용인·수원·성남·고양·부천·안산·안양·화성·청주·천안·전주·포항·창원에 적용하며, 서울 등 광역시는 기존 시·도 아래 구를 바로 선택합니다. 인천 구 목록은 [인천광역시 공식 자치군·구 목록](https://www.incheon.go.kr/index)을 참고했습니다. 기존 회원 주소를 일괄 변경하지 않습니다.
7. 회원가입에 선택 입력인 추천인코드를 추가했습니다. 마이페이지에 자신의 숫자 코드, 복사 버튼, 해당 코드로 가입한 인원을 표시합니다. 기존 회원은 저장된 가입일 순, 이후 회원은 가입 저장 순으로 번호를 발급합니다. 동일 가입일은 ID 순, 가입일이 없는 회원은 마지막에 ID 순으로 배치합니다. 번호는 재사용하지 않으며 관리자에 의한 아이디 변경 후에도 유지합니다. 추천 이벤트 보상 조건과 지급 기능은 아직 포함하지 않습니다.

## 데이터 적용 시점

- 급상승은 이전과 현재 게시 순위가 있어야 합니다. 기존 게시본에 상승 비교값이 없으면 31.39에서 다음 수집·게시 후 나타납니다. 이전 게시본 자체가 없었다면 두 번째 게시가 필요합니다.
- 정치키워드는 대표 기사 10개만 저장된 과거 스냅샷으로 비교 숫자를 만들지 않습니다. 31.39로 새 수집·게시해야 전체 수집 제목을 이용한 비교가 나옵니다. 실제 수집된 기사 범위의 집계입니다.
- 키워드 비교용으로 최근 14일 수집 기사 제목·매체·일시를 보존하며 본문은 보존하지 않습니다. 관련 기사 링크는 대표 자료 범위로 제한합니다. 이는 기존 뉴스 관측 지문 저장과 별개이며 기존 스냅샷 정리 정책을 따릅니다.
- 추천인 번호는 배포 후 최초 회원 세션 확인 또는 신규 가입 때 기존 회원에게 먼저 자동 부여됩니다. Redis의 단일 Lua 처리로 신규 가입과 번호 발급을 함께 저장합니다. 다른 회원 정보 저장이 동시에 발생하면 오래된 전체 회원 목록으로 덮어쓰지 않고 재시도를 안내합니다.

## 배포

1. `JCS_0_0_31_39.zip`의 파일을 기존 저장소 같은 경로에 덮어쓰고 재배포합니다. 새 프로젝트를 만드는 전체 자산 ZIP이 아니라 기존 프로젝트에 적용하는 배포본입니다.
2. 관리자 화면과 `/api/v3/health`에서 `JCS_0_0_31_39` 확인 후 강력 새로고침합니다.
3. `관리자 → 풋터정보`에 정참시 사업자 정보를 입력하고 저장합니다.
4. 기존 정치인 데이터 수집·게시 절차를 진행하면 급상승 비교와 정치키워드가 갱신됩니다. 수집 탭을 열어 둔 동안 분할 호출이 진행됩니다.
5. 회원가입에서 서울 구 선택, 용인 시·구 선택, 추천인코드 입력을 확인하고 마이페이지에서 코드와 추천 인원을 확인합니다.
6. 기존 폐기 CSS 목록은 `DELETE_OBSOLETE_FILES_31_39.txt`를 참고합니다. 이번 버전에 새로 삭제할 런타임 파일은 없습니다.

## 검증 범위

자동 테스트 484개 통과, 수정 JavaScript 문법 검사, 독립 코드 리뷰 및 수정 후 재검토를 수행했습니다. 추천인 처리는 실제 운영 Lua 코드를 로컬 Lua 런타임에서 실행해 검증했습니다. 테스트와 변경 목록은 별도 검증 ZIP에 들어 있습니다.

실제 운영 서버 배포, 실기기 폴드 렌더링, 운영 Redis/Blob 접속 및 운영 환경 응답 시간 측정은 수행하지 않았습니다. 관리자 성능 개선은 탭별 API 호출 수와 캐시 동작으로 검증했으며 실제 화면 전환 시간을 측정한 수치는 아닙니다.
