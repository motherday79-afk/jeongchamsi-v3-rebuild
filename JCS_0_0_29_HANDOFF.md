# JCS 0.0.29 정치 인텔리전스 2단계 패치 인수인계

## A. 패치 기준

- 기준 버전: JCS 0.0.28
- 적용 버전: JCS 0.0.29
- 적용 방식: ZIP을 프로젝트 루트에 덮어쓴 뒤 배포
- GitHub 등록: 사용자 직접 진행

## B. 핵심 구조

- 관리자 상세페이지를 `정치 진단 10개 → 진단 요약 → 전환부 → 전략 처방 10개 → 실행 우선순위` 순서로 분리했다.
- 같은 항목 안에 진단과 처방을 섞지 않는다.
- 비로그인 3개(01·07·09), 일반회원 6개(01·02·03·05·07·09), 관리자 10개 전체 권한을 중앙 설정에서 관리한다.

## C. 관리자 상세페이지

- 10개 진단 각각에 현재 위치, 변화 흐름, 근거, 비교 기준, 해석, 기회, 위험을 표시한다.
- 진단 뒤에 가장 강한 자산, 구조적 취약점, 성장 변수를 요약한다.
- 처방 10개는 목표, 판단, 실행안, 타깃, 메시지, 채널, 시점, 예상 변화, 추적 지표를 표시한다.

## D. 관리자 비교하기

- 최소 2명, 최대 4명 비교를 유지한다.
- 10개 동일 항목을 고정 프로필 헤더형 비교 매트릭스로 표시한다.
- 전략 기준 정치인을 선택하며 기본값은 첫 번째 정치인이다.
- 기준 정치인을 바꾸면 해당 정치인의 처방 10개와 우선순위를 표시한다.

## E. 회원·비로그인 화면

- 비로그인은 01·07·09 진단만 표시하고 로그인 진입을 제공한다.
- 일반회원은 01·02·03·05·07·09 진단의 수치, 변화, 비교, 해석까지만 표시한다.
- 두 권한 모두 관리자 처방 데이터와 전략 판단을 받지 않는다.

## F. 서버 권한 보안

- 서버에서 역할을 판별한 뒤 허용 필드만 투영한다.
- 비로그인·회원 응답에 `prescriptions`, 관리자 우선순위, 관리자 뉴스 서사를 포함하지 않는다.
- 알 수 없는 중첩 속성도 화이트리스트 밖이면 제거한다.
- 응답 캐시는 기존과 같이 `Cache-Control: no-store`를 유지한다.

## G. 구형 스냅샷 호환

- 공개 저장소에 JCS V1 스냅샷이 남아 있어도 API 게이트웨이가 현재 프로필과 기존 뉴스·순위를 보존하며 V2 진단/처방을 결정적으로 구성한다.
- 신규 V2 스냅샷은 그대로 사용한다.

## H. 뉴스 헤드라인 처리

- 유사 기사 중복 제거, 매체당 최대 2건, 전체 최대 10건을 적용한다.
- 의제, 프레임, 정치인 주도성, 연결 진단 번호를 부여한다.
- 화면에서는 기본 5건과 확장 5건으로 표시한다.

## I. 데이터 원칙

- 랜덤 수치와 임시 숫자를 사용하지 않는다.
- 직접 데이터, 파생 데이터, 정치 구조 데이터를 구분해 기록한다.
- 뉴스가 적어도 공식 직책·정당·지역·선수·정치 구조로 전체 보고서를 안정적으로 구성한다.
- JCS 상대지수를 공식 지지율이나 당선 확률로 표현하지 않는다.

## J. NOW 순위

- 기존 NOW 계산식과 공개 순위 계산 로직은 변경하지 않았다.
- 상세페이지와 비교하기는 같은 발행 보고서 값을 사용한다.

## K. 최신 직책 반영

- 김민석: 2026-08-17 더불어민주당 대표를 주 직책으로, 제22대 국회의원·서울 영등포구 을을 보조 직책으로 유지했다.
- 최민희·박선원·서미화·이성윤·한민수: 2026-08-17 최고위원 역할을 현직 의원 정보와 함께 반영했다.
- 등록 인물 중 이소영·김승원·용혜인은 2026-08-30 장관 후보자 상태를 `nominated`로 반영했다.
- 모든 역할에 기준일, 상태, 확인일, 출처 식별자를 저장한다.

## L. 시각화

- 진단 10개와 처방 10개에 서로 다른 시각화 타입을 부여했다.
- CSS 기반 경량 막대·행렬·타임라인 표현을 사용한다.
- 관리자 카드의 제목과 여백을 공개 상세페이지와 비슷한 고밀도로 유지한다.

## M. 반응형

- 상세페이지 모바일에서는 요약, 처방, 우선순위가 한 열로 전환된다.
- 관리자 3·4인 비교는 데스크톱 고정 헤더와 가로 스크롤을 사용한다.
- 모바일은 안전한 가로 비교 매트릭스와 세로형 처방 카드로 전환된다.

## N. 사진·기존 기능

- 상세 및 비교 프로필 사진 연결 방식을 유지했다.
- 기본 프로필, 정치 기록, 경력, 탭, 최근 본 정치인, 검색, 자동완성, 비교 추가/삭제를 보존했다.

## O. 실행 우선순위

- 즉시 실행 3개
- 30일 이내 실행 3개
- 90일 이내 실행 2개
- 중장기 관리 2개

## P. 상세페이지 검수

- 비로그인: 진단 3개 및 처방 미노출 테스트 통과
- 일반회원: 진단 6개 및 처방 미노출 테스트 통과
- 관리자: 진단 10개, 요약, 전환부, 처방 10개, 3·3·2·2 우선순위 테스트 통과
- 로컬 브라우저 실행 파일이 없어 실제 PNG 캡처는 생성하지 못했으며, DOM·CSS 렌더링 및 모바일 분기 테스트로 검수했다.

## Q. 비교하기 검수

- 비로그인·회원 2인 제한 테스트 통과
- 관리자 2~4인 선택, 4인 헤더, 10개 비교 항목, 기준 정치인 변경 테스트 통과
- 로컬 브라우저 실행 파일이 없어 실제 PNG 캡처는 생성하지 못했으며, 가로 넘침 방지 규칙과 모바일 전환을 테스트했다.

## R. 전수·회귀 테스트

- 전체 테스트: 217개 통과, 실패 0개
- 등록 정치인: 542명 전원 진단 10개·처방 10개 생성 통과
- 543개 슬롯 중 공석 1개는 분석 대상에서 제외
- 100개가 넘는 서로 다른 점수 벡터를 확인해 단일 복제값 사용을 차단

## S. 현재 실제 데이터 한계

- 외부 원자료가 적은 정치인은 공개 프로필·역할·정당·지역·선수·정치 구조 중심으로 해석한다.
- 새로운 유료 API나 외부 수집기는 추가하지 않았다.
- 기존 저장소에 발행된 V1 보고서는 요청 시 V2로 호환 구성되며, 다음 정식 수집·발행 이후 V2 원본으로 교체된다.

## T. 적용 후 확인

1. 프로젝트 루트에 ZIP 내용을 덮어쓴다.
2. `npm test`를 실행한다.
3. 배포 후 강력 새로고침한다.
4. 비로그인, 회원, 관리자 계정으로 상세페이지와 비교하기를 각각 확인한다.
5. 관리자 비교에서 4명을 선택하고 전략 기준 정치인을 변경한다.

