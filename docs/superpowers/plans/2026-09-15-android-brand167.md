# 정참시 Android 브랜드 167 구현 계획

승인: 신규 앱 아이콘 포함 빌드 프로젝트 준비. 기존 최종 인트로 원본과 2026-09-13 뒤로가기 패치를 기준으로 한다.

## 확정 사양
- 기존 골드 8광선 PNG의 픽셀·비율 유지. 딥 퍼플 #241039 배경의 Adaptive icon.
- 기존 순서: 광선 결합 → 참여하는 정치의 시작 → 정참시. 2.3초 동안 절도 있는 진입 후 골드 빛의 회전과 텍스트 스윕.
- 실제 로고를 고정하고 빛만 움직인다. 웹은 동시에 로딩하며 실제 콘텐츠 준비 후 자연스럽게 전환. 최대 15초 뒤 재시도 UI.
- 앱 ID com.jeongchamsi.minimal, 기존 뒤로가기·쿠키·외부 링크 정책 유지.
- 원본 인트로 DEX는 참고 기록으로 보존하고 새 Canvas 소스로 명시적으로 대체한다. 런타임에 옛 인트로 중복 포함하지 않는다.
- 웹 167은 케이지 실제 글리프 블록 중앙정렬과 앱 준비 상태 신호만 변경. 이미지·데이터·진영 선택·기존 줄바꿈 승인 범위 유지.

## 작업
1. 회귀 테스트: StartupGate/IntroTimeline Java 경계값, 웹 글리프 중심 정렬.
2. Native IntroView 구현 및 런처 XML/원본 PNG 리소스, 앱 시작/종료 수명주기 연결.
3. SDK35 AAPT2 javac D8 빌드 경로에서 원본 DEX 병합 제거, 새 클래스 컴파일·검증. 새 버전/공식 URL 설정.
4. 웹 제목 측정값으로 두 줄 묶음 중앙배치 및 콘텐츠 준비 마커. 쿼리 없는 API 공유 import 유지.
5. 실제 Android SDK 빌드·서명 검증 시도, 단위 테스트/웹 전체 회귀 검증. 실기 확인 여부 명확히 구분.
6. 소스 ZIP/GitHub Actions/한국어 빌드 안내와 웹 167 PATCH/FULL 패키지, 검증 기록 포함.
