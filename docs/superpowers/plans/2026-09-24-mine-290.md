# Mine 290 — reference-matched pickaxe effects

User authorizes implementation and deployment; user performs visual/play verification. Preserve current checkout, sprites, economy and account state.

1. Generate a transparent six-cell VFX atlas matching approved mystic/dimension/dark/heaven/lightning/wind concept motifs. Preserve weapon silhouettes in integrated sprites. Use raster wisps/feathers/lightning/ghost weapons, not generic circles alone.
2. Record weapon head and handle direction for each frame/race using source sprite geometry. Attach effects to head and shaft; share per-theme textures across races.
3. Persistent equipped aura remains visible while idle. Stop timers when hidden/unloaded; bounded particles and selected-texture loading only. Swing trails and contact effects remain separate, with reduced-motion support.
4. Replace generic effects with texture-based themes, tune native frame scale and separate source-over dark smoke from additive light.
5. Syntax check only per user. Version 290, push, package separate patch/full archives. Do not claim on-device visual review.
