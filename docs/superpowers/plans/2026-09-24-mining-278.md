# Mining campaign / scratch card 278

User authorized implementation and deployment in the current working directory, without repeated visual verification. Keep existing style, character motion and 1.5s/worker mode. Deliver PATCH and FULL archives.

## Contract / scope

- Lottery costs 20 mine G and uses server-side independent 5% probability. Initial prize movie ticket, global maximum 50 wins. Only lottery closes at the cap; mining continues. Admin may edit cap and start next advertiser round.
- Starting a new round resets gold, ore, ordinary pick/worker/storage upgrades for all accounts on next access; paid pick ownership and choice of character survive. Existing free trial is not a purchase. Preserve durable old prize claims for fulfillment.
- First migration into initial round preserves existing 277 balances. No unauthorized production reset merely by deploying.
- Lottery purchase and currency/global win count are atomic, idempotent, server validated. One unrevealed ticket at a time. Persist result across refresh. Expose personal plays/wins, 5%, overall remaining prizes, and durable prize history to owner; winners to admin only.
- Admin start/update checks current campaign id and request id to prevent duplicate reset and stale writes. Keep historical rounds and prize claims. UI requires explicit confirmation on reset.
- Frontend contract: `campaign {id,title,prize,limit,wins,status,remaining,cost:20,chance:.05}`, `lottery {plays,wins,ticket,history}`, ticket `{id,won,prize,at,revealed,campaignId,code}`. Returned with existing state/ad/jackpot. `state.campaignId` identifies reset.
- `lottery-buy {campaignId,expectedPlays}` / `lottery-reveal {campaignId,ticketId}` use existing requestId action transport. `mine/admin` supports `campaign-update {campaignId,title,prize,limit}`, `campaign-start {campaignId,requestId,title,prize,limit,ad:{name,message,url,enabled}}`. Admin GET adds campaign, winners, campaigns. Existing ad saving remains.
- Sponsor sign becomes real accessible link to configured URL, same tab; collection redirect retained. No fabricated ad delivery counts for direct link.
- Replace isolated ore pebble with a substantial connected foreground seam, keep resting worker and selected character. Show pick tier in equipment illustration, plus actual in-play pick visual upgrade without recoloring entire character.

## Tasks

1. Backend implementer: campaign state, atomic lottery actions/reset/claims/admin, paid ownership preservation, focused concurrency/idempotency/reset tests. Own `lib/mining*`, new backend modules, tests. Do not edit frontend or commit/push.
2. Main agent: scratch canvas/ticket/result/statistics UI, admin round forms and winners, sponsor link, new seam and equipment visuals. Own `mine/*`, assets, documentation/version/packaging. Use imagegen for raster assets.
3. One fresh code review of final changes, targeted test suite and syntax check; address material findings. No prolonged production playtest.
4. Commit/push authorized main, package from 277 FULL + patch, verify equality, report actual push and test results, user performs live playtest.

## Rulings

- New round also resets worker/storage and current lottery counters because the user describes all ordinary game growth reset. Lifetime prize history remains.
- Paid picks use trusted server ownership only; no new charge or invented existing entitlement.
- Prize means claim record, not an actual cinema voucher issued by an unconnected vendor. Admin winners view provides fulfillment record.
