# Groups 171 implementation plan

**Goal:** Deliver approved administrator-reviewed group communities with three fictional examples.
**Architecture:** Per-group optimistic-concurrency records, compact discovery index and per-user group references; server-enforced group roles and visibility. Private media storage with authenticated proxy; no public gallery URLs or global-role changes. New routed views use the established shell and navigation.
**Tech stack:** Existing JavaScript/Redis/Vercel Blob/CSS/Node tests. No new package dependency.
**Spec:** ../specs/2026-09-15-groups.md

## Tasks
1. Root: write permission/concurrency/example tests before group-model and group-service; implement scoped membership, moderated content, events and in-app news. HTTP dispatcher uses actual session user and rejects cross-origin writes. Public serializers never include private records or storage paths.
2. Frontend implementer: read the exact spec contract; create six assigned UI/client/test files. Use test-first for payload and permission-sensitive rendering. Build compact directory, my groups, review screen, group tabs and actionable forms. Do not modify shared entry points.
3. Root: create fictional example data and six-scene image atlas, wire modules into existing app/gateway/service icon/admin entry, update cache references to171. Preserve170 files outside the integration changes.
4. Review: check whole subsystem for ACL, concurrent writes, media authorization, UI mismatch and regressions; fix concrete findings and rerun impacted tests.
5. Verify all tests; package171 patch against170 and full ZIP, include setup/verification documentation and preserve approval assets. Save deliverables and state deployment/device-test limits.
