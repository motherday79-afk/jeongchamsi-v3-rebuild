# JCS 0.0.31.36 적용 안내

## 적용 순서

1. 현재 운영 저장소를 초기화하거나 다시 체크아웃하지 않습니다.
2. ZIP을 풀어 기존 프로젝트의 같은 경로에 파일을 반영합니다.
3. `DELETE_OBSOLETE_FILES_31_36.txt`에 적힌 구형 CSS 파일이 운영 저장소에 남아 있으면 삭제합니다.
4. 변경 사항을 커밋하고 배포합니다.
5. GitHub Actions가 자동 실행되지 않았을 때만 `Run workflow`를 누릅니다.
6. 배포 후 관리자 화면에서 `JCS_0_0_31_36`이 표시되는지 확인합니다.

## 이번 변경

- 메인 서비스 내비게이션을 아이콘 타일과 하단 라벨 방식으로 교체했습니다.
- 메인 9개 영역의 우측 상단 글자 버튼을 기능별 아이콘 버튼으로 교체했습니다.
- PC NOW Rank 화살표를 카드 바깥 여백으로 이동했습니다.
- 모바일 NOW Rank를 화살표 없는 2장 가로 스와이프 방식으로 교체했습니다.
- 모든 클릭 대상에 손 모양 커서를 적용했습니다.
- 사이드바 배너·MY·실시간 키워드·최근 본 정치인·NEWS·급상승 정치인의 바깥 박스 호버를 제거하고 실제 항목에만 호버를 남겼습니다.
- 헤더의 중복 배지·최근 본 정치인 아이콘을 삭제하고 검색창을 넓혔습니다.
- 풋터 이용안내를 삭제하고 개인정보처리방침·운영정책을 구체화했습니다.
- 관리자 `풋터정보` 메뉴에서 운영·사업자 정보를 저장하면 비어 있지 않은 항목만 공개 풋터에 표시됩니다.

## 환경설정

- 이번 풋터 정보 저장은 기존 Redis 연결을 사용하며 추가 환경변수가 필요하지 않습니다.
- 정치인 사진과 메인 배너 업로드에는 기존 Vercel Blob 연결이 계속 필요합니다.
- 메인 배너 권장 크기는 640 × 450px, 최대 용량은 2MB입니다.
- 정치인 사진은 JPG·PNG·WEBP, 최대 1MB입니다.

## 검증 결과

- Node 자동 테스트: 457개 전체 통과
- JavaScript 구문 검사: 통과
- 새 배포 ZIP: 이미지 파일 제외, 100개 미만 파일 구성
