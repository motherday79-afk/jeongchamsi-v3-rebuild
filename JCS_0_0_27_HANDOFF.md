# JCS 0.0.27 적용 안내

## 적용 방법

1. 현재 GitHub 저장소를 먼저 백업하거나 새 브랜치를 만듭니다.
2. `JCS_0_0_27_NAVIGATION_LEGACY_RESTORATION_PATCH.zip`을 저장소 최상위 폴더에서 풉니다.
3. 같은 이름의 파일은 모두 덮어씁니다. ZIP 안의 폴더 구조는 저장소 기준 상대 경로입니다.
4. 데이터 마이그레이션은 다시 실행하지 않습니다. 이번 패치는 이미 이식된 `polls`, `generation`, `nationalEvaluation` 데이터를 그대로 읽습니다.
5. 배포 후 PC와 모바일 브라우저에서 강력 새로고침을 한 번 실행합니다. 버전과 캐시 키는 `0.0.27`입니다.

## 이번 패치 내용

- 브라우저 뒤로가기/앞으로가기 시 직전 화면 HTML, URL 상태, 스크롤 위치를 즉시 복원합니다.
- 느린 이전 요청이 현재 화면을 덮는 비동기 렌더 경쟁을 차단합니다.
- `정참시민 전국 평가제`의 2개 평가 슬롯, 실제 정치인 이름·사진, 결과 비율, 지난 평가와 투표를 복구했습니다.
- `세대의 선택, 대통령`의 연령 탭, TOP 15, 정치인 검색·선택, 기존 투표 데이터를 복구했습니다.
- `귀담아 들어야 합니다`를 전체 설문 게시판으로 복구하고, 선택 후 `투표 확인`을 누르는 방식으로 변경했습니다.
- `전체 서비스`는 삼선 메뉴를 열지 않고 현재 `EXPLORE JEONGCHAMSI` 영역 아래로 13개 서비스를 펼칩니다.
- 통합검색에 정치인, 대통령·정부, COLUMN, IT'S ME, 정뮤니티, 시민 설문, 세대 선택, 전국 평가 결과를 연결했습니다.
- 정치인 비교는 정치인을 선택한 뒤 `비교하기`를 눌러야 분석이 표시됩니다. 관리자는 최대 5명까지 선택할 수 있습니다.
- 관리자 3~5인 비교를 핵심 비교표, 인물별 컨설팅 진단, 접힌 상세 지표의 간결한 구조로 변경했습니다.
- 대통령 페이지에 이전판의 프로필, 경력, 선거, 비전, 정책, 공약, 국정과제, 리더십, 정부 구성 데이터를 복구했습니다.
- 정치인 상세의 글자와 박스 크기를 이전판 수준으로 축소했습니다.
- 정치인 상세 NOW 카드는 `전체 NOW`와 `분야별 NOW`만 남긴 정사각형 2열 카드로 바꿨습니다. 순위는 `N위`, 미집계는 `집계 전`으로 표시합니다.
- `공개 스냅샷 운영 순위`, `국회의원 NOW 독립 순위` 설명은 모든 정치인 상세에서 삭제했습니다.
- `정참시 응원하기` 소개문을 요청한 원문과 줄바꿈으로 교체하고 첫 문장과 마지막 문장을 강조했습니다.
- 이전 `0.0.26.1` PC 흰 화면 초기 로딩 보호와 배지 변경을 함께 포함했습니다.

## 확인 권장 경로

- `/` → 전체 서비스 펼치기/접기, 세대 선택 이름 표시
- `/poll` → 여러 설문, 항목 선택 후 투표 확인
- `/generation-president?age=20대` → 연령 탭, TOP 15, 검색
- `/national-evaluation` → SLOT A/B와 지난 평가
- `/president` → 대통령·정부 전체 정보
- `/search?q=이재명` → 그룹별 통합검색
- `/compare` → 2~5명 선택 후에만 비교 결과 표시
- `/person/assembly-001` → 정사각형 NOW 카드와 축소된 상세 레이아웃
- 여러 페이지 이동 후 브라우저 뒤로가기 → 직전 화면과 스크롤 즉시 복원

## 검증 결과

- JavaScript 변경 파일 전체 `node --check` 통과
- `git diff --check` 통과
- 전체 테스트: 178개 중 177개 통과
- 남은 1개는 이번 패치 이전부터 존재한 사진 파일 개수 기준 불일치입니다. 테스트 기준은 515장이지만 현재 저장소의 사진 폴더에는 593장이 있습니다. 사용자 자산을 임의 삭제하지 않았으며 이번 기능 변경과 무관합니다.

## 되돌리기

적용 전에 만든 브랜치 또는 백업으로 되돌리십시오. 이 패치는 데이터 마이그레이션이나 Redis 데이터 삭제·덮어쓰기를 실행하지 않습니다.
