# JCS 0.0.31.40 적용 안내

31.39를 기반으로 승인된 화면 수정과 수집 오류 개선을 반영한 배포본입니다. 기존 프로젝트에 같은 경로로 적용하는 99개 파일이며, 전체 이미지 자산을 포함한 신규 프로젝트 ZIP은 아닙니다.

## 변경 내용

1. 메인 NOW Rank: PC의 `1–10 / 100` 표시를 다섯 번째 카드 위 오른쪽으로 이동했습니다. 화살표는 카드 바깥의 좌우 전용 공간을 유지합니다. 카드 높이는 PC 174→150px, 모바일 180→156px로 줄이고 사진과 내부 여백도 함께 조정했습니다.
2. 상세 02: 연령별 다섯 막대 안의 남성·여성 비율을 위아래로 쌓습니다. 연령별 전체 비율은 꼭대기, 성별 수치는 각 구간 안에 표시합니다.
3. 상세 점수 단위를 `점`에서 `P`로 변경했습니다. %, 건, 명, 위, K는 유지합니다.
4. 상세 03: 지역 의제 대응 매트릭스를 `지역 의제별 활동 현황`으로 교체했습니다. 전체 수집 기사 중 지역 관련 기사 건수를 가로 막대로 보여주고, 본인 주도로 분류된 기사 건수를 진한 부분과 숫자로 표시합니다. 의제별 최신 근거 링크를 연결합니다. 최대 5개 의제의 집계와 최신 근거를 기존 압축 스냅샷에 저장해 재조회 시 표본 축소로 숫자가 변하지 않도록 했습니다.
5. 상세 04: 독립지수 합계와 합계 100이 아니라는 안내 문구를 제거했습니다.
6. 상세 06: 이슈 지속력의 추세선·영역·강조점을 제거하고 일별 막대만 표시합니다. 기사량에 따라 연한색·중간색·진한색을 적용합니다. 날짜·건수·수집 여부와 근거 정보는 유지합니다.
7. 상세 07: 매체 집중도의 `yna.co.kr`, `www.yna.co.kr`을 `연합뉴스`로 표시합니다.
8. 관리자 수집: 대량 기사 입력이 모든 세대·성별 값을 상한/하한으로 몰아 `COHORT_VECTOR_CLONED`를 일으키는 계산을 수정했습니다. 기사 긍정·부정 차이를 전체 기사 수로 정규화해 최대 ±14의 영향으로 제한합니다. 검증 규칙 자체는 유지합니다. 새 수집 배치는 25명에서 10명으로 줄이고, 동시에 처리하는 인원은 5명입니다. 일시적 통신 오류를 재시도하며 중간 저장이 끝난 인물은 같은 작업 재개 시 다시 수집하지 않습니다. 같은 브라우저 페이지에서 다른 관리자 탭으로 이동해도 수집은 계속됩니다.
9. 풋터: 운영·사업자명, 대표자, 사업자등록번호 등 제목과 값을 모두 11px로 통일했습니다. 항목별 강제 행 묶음을 없애고 가로로 이어 배치하며 공간이 부족할 때만 줄바꿈합니다. 항목 사이 구분선과 작은 행 간격을 적용하고 전체 상하 여백도 줄였습니다.

## 적용 순서

1. 배포 ZIP 파일을 기존 저장소의 동일 경로에 덮어쓰고 재배포합니다.
2. `/api/v3/health`와 관리자 버전 표시에서 `JCS_0_0_31_40`을 확인하고 강력 새로고침합니다.
3. 수집 오류 목록에서 기존 실패 항목을 재시도합니다. 오류 해결 확인 후 기존 검토·게시 절차를 따릅니다. 계산 개선은 새 분석 또는 재분석에 반영됩니다.
4. 지역 의제 전체 기사 집계는 이번 버전으로 수집·게시한 스냅샷부터 보존됩니다. 과거 압축 스냅샷에 없는 원본 기사를 복원하거나 임의로 건수를 늘리지 않습니다.
5. PC, 접은 폴드, 펼친 폴드에서 NOW 카드/화살표/페이지 표시와 풋터 줄바꿈을 확인합니다. 관리자 풋터정보의 실제 입력값으로 확인합니다.
6. 폐기 파일 안내는 `DELETE_OBSOLETE_FILES_31_40.txt`에 있습니다. 이번 버전에서 새로 삭제해야 하는 런타임 파일은 없습니다.

## 수집 동작 범위

수집 탭을 떠나도 동일 페이지가 열려 있는 동안 분할 요청이 이어집니다. 브라우저 종료나 운영체제의 백그라운드 중단까지 서버 예약 작업처럼 계속 실행되는 기능은 아닙니다. 재접속하면 저장된 작업 위치에서 재개합니다. 일시적 오류가 반복되거나 외부 제공자가 실패하면 오류 목록에 남으며, 성공한 항목 전체를 초기화할 필요가 없습니다. 배포 전 이미 청구된 구버전 배치는 기존 크기를 유지할 수 있습니다.

## 검증

전체 자동 테스트, 배포 JavaScript 문법 검사, ZIP 무결성 검사와 독립 코드 재검토를 수행합니다. 상세 결과·테스트 코드·변경 파일 해시는 별도 검증 ZIP에 포함합니다.

실제 운영 서버 배포, 운영 Redis/외부 수집 API 접속, 실기기 폴드 렌더링과 운영 환경 속도 측정은 수행하지 않았습니다. 로컬에서 재현한 계산·저장·재시도 오류와 렌더링 결과를 검증한 배포본입니다.
