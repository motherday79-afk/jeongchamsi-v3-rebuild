# JCS 0.0.31.10 페이지별 Open Graph 공유카드 패치

## 주요 변경

- 메인, 정치인, 칼럼, 뉴스, IT’S ME, 정뮤니티, 아카데미, 전국평가제, 세대의 선택, 대통령, 비교하기에 서로 다른 OG 메타데이터를 적용했습니다.
- `/person/:id`, `/column/:id`, `/compare?...` 등 공개 일반 URL을 서버 메타데이터 응답으로 연결했습니다.
- 기존 `#/...` 주소는 접속 시 같은 일반 URL로 전환되며 기존 SPA 화면, 뒤로가기와 데이터 흐름을 유지합니다.
- 정치인 카드는 등록된 기존 프로필 사진을 사용하고, 게시물은 자체 대표 이미지를 우선 사용합니다.
- 대표 이미지가 없거나 안전하지 않은 경로이면 섹션별 1200×630 PNG, 마지막으로 메인 PNG를 사용합니다.
- 비교 카드에는 공개 대상 2명의 이름과 공개 프로필만 사용하며 관리자 비교 파라미터와 인텔리전스는 제거합니다.
- canonical, `og:url`, 내부 생성 공유 URL은 `https://www.jeongchamsi.com`으로 고정했습니다.

## 서버 처리

- `api/share.js`는 로그인 쿠키와 관계없이 공개 정치인 프로필과 공개 게시물 필드만 읽습니다.
- 관리자 진단, 처방, 취약점, 전략 데이터 저장소는 호출하지 않습니다.
- JavaScript 실행 전 HTML `<head>`에 Open Graph와 Twitter Card 메타태그가 완성됩니다.
- Redis 또는 콘텐츠 조회 장애 시 빈 태그를 반환하지 않고 정참시 메인 카드로 안전하게 fallback합니다.

## 검수 범위

- 메인 카드
- 서로 다른 정치인 3명 카드
- 서로 다른 칼럼 및 대표 이미지 없는 칼럼 fallback
- 뉴스, IT’S ME, 정뮤니티, 아카데미
- 전국평가제, 세대의 선택, 대통령
- 공개 2인 비교 및 관리자 파라미터 제거
- canonical 공식 도메인, 빈 설명 방지, 관리자 필드 미노출
- 섹션별 PNG 11종의 실제 1200×630 규격
- 기존 전체 자동 테스트

## 배포 후 확인

1. GitHub에 ZIP 내부 경로 그대로 덮어씁니다.
2. Vercel 배포 완료 후 관리자 화면 버전 `JCS_0_0_31_10`을 확인합니다.
3. 기존 `#/...` URL을 한 번 열면 일반 URL로 바뀌는지 확인합니다.
4. 카카오톡에 일반 URL을 공유합니다. 과거 캐시가 남아 있으면 카카오 공유 디버거에서 해당 URL 캐시를 갱신합니다.

