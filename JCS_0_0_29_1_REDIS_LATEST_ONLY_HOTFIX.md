# JCS 0.0.29.1 Redis 최신 데이터 전용 핫픽스

## 적용 기준

- 기준 버전: JCS 0.0.29
- 패치 버전: JCS 0.0.29.1
- 목적: 관리자 인텔리전스 수집이 Redis 용량 한계로 350/542 부근에서 멈추는 문제 해결

## 핵심 변경

1. Redis에는 정치인별 전체 진단서가 아니라 현재 화면 재구성 및 순위 계산에 필요한 입력만 저장합니다.
2. 이전 인텔리전스 이력은 저장하지 않으며, 기존 인텔리전스 이력 키도 안전하게 정리합니다.
3. 현재 공개 중인 스냅샷은 새 수집이 완전히 검증될 때까지 유지합니다.
4. 새 수집·공개가 완료되면 최신 스냅샷만 남기고 이전 인텔리전스 스냅샷을 정리합니다.
5. 사용자, 게시물, 댓글, 투표, 배지, 참여 이력은 삭제 대상에 포함하지 않습니다.
6. API 요청 시 저장된 최신 입력을 이용해 기존 10개 진단·10개 처방 구조를 다시 생성하므로 권한별 상세·비교 화면은 유지됩니다.

## 멈춘 350/542 작업의 처리

- 배포 후 관리자 페이지에서 수집을 다시 실행하면 기존 `COMPACT_V2` 미완료 작업을 자동 감지합니다.
- 해당 미완료 스냅샷의 조각만 삭제하고 진행률을 `0/542`로 재설정합니다.
- 현재 공개 데이터와 회원·콘텐츠 데이터는 그대로 유지됩니다.
- Redis 데이터베이스 전체를 수동 초기화하지 마십시오.

## 용량 검증

동일한 542명 분석 결과를 직렬화해 비교했습니다.

- 기존 전체 보고서 저장 예상치: 25.17 MiB
- 최신 입력 전용 저장 예상치: 1.43 MiB
- 감소율: 94.3%
- 정치인 1명 최대 축약 레코드: 2,886 bytes

## 적용 후 순서

1. 이 ZIP의 파일을 JCS 0.0.29 저장소 루트에 덮어씁니다.
2. 배포가 완료되면 관리자 페이지를 새로고침합니다.
3. 인텔리전스 수집을 한 번 실행하고 `542/542` 완료까지 기다립니다.
4. 수집 검증 완료 후 공개를 한 번 실행합니다.
5. 브라우저 강력 새로고침으로 화면을 확인합니다.

## 검수 결과

- 전체 자동 테스트: 219개 통과, 실패 0개
- 542명 수집: 25명 이하 배치 22회 완료 검증
- 기존 350/542 미완료 작업 자동 초기화 검증
- 최신 스냅샷 1개만 유지 검증
- 인텔리전스 이력 제거 및 사용자·공개 스냅샷 보존 검증
- 전체 분석 검증 후 저장용 축약이 수행되는 순서 검증

## 변경 파일

- `lib/intelligence-storage.js`
- `lib/intelligence-repository.js`
- `lib/intelligence-service.js`
- `api/gateway.js`
- `package.json`
- `package-lock.json`
- `index.html`
- `src/app.js`
- `tests/intelligence-analysis.test.js`
- `tests/intelligence-repository.test.js`
- `tests/intelligence-service.test.js`
- `tests/politicians.test.js`
- `JCS_0_0_29_1_REDIS_LATEST_ONLY_HOTFIX.md`
- `JCS_0_0_29_1_MANIFEST.txt`
