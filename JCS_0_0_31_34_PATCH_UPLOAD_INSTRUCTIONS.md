# JCS 0.0.31.34 소형 패치 업로드

이 ZIP은 사진 파일을 다시 포함하지 않는 소형 덮어쓰기 패치입니다. 현재 정참시 0.0.31.33 프로젝트와 기존 정치인 사진을 그대로 유지합니다.

1. ZIP을 PC에서 압축 해제합니다.
2. 압축 안의 `css`, `lib`, `src`, `tests` 폴더와 루트 안내 파일을 저장소 최상단에 같은 경로로 올립니다.
3. 같은 이름의 파일은 모두 덮어씁니다. 기존 사진·환경변수·데이터 파일은 삭제하지 않습니다.
4. 커밋 후 배포 작업이 자동으로 시작되는지 확인합니다.
5. 자동 실행되지 않을 때만 해당 GitHub Actions 배포 워크플로에서 `Run workflow`를 누릅니다.
6. 배포가 끝나면 브라우저를 강력 새로고침하고 관리자 화면 버전 `JCS_0_0_31_34`를 확인합니다.
7. 기존 게시본의 상세 그래프와 공식 유권자 데이터를 갱신하려면 대상 정치인을 `한 명 수집 → 게시`합니다.

ZIP에는 이미지, `node_modules`, 환경변수, 인증정보, 과거 ZIP을 포함하지 않았습니다. 따라서 100개 이상 사진 업로드 제한과 충돌하지 않습니다.

